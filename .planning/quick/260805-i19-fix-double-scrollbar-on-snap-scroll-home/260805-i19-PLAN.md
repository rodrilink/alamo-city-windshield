---
phase: quick
plan: 260805-i19
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(public)/layout.tsx
  - src/app/(public)/page.tsx
  - src/app/(public)/about/page.tsx
  - src/app/(public)/contact/page.tsx
  - src/components/layout/TopNav.tsx
  - src/components/home/ServicesSection.tsx
autonomous: false
requirements: [I19]

must_haves:
  truths:
    - "The home page (/) shows exactly one vertical scrollbar, and it belongs to the snap container."
    - "Scrolling the home page with the wheel snaps between full-viewport sections as before."
    - "TopNav is visible and clickable on top of the hero photo on / without occupying vertical layout space."
    - "/about and /contact still render TopNav in normal flow above content and Footer in normal flow below it, and scroll as ordinary documents."
    - "The Footer is a fourth snap stop on / — reachable by snapping past ServicesSection, rendering BUSINESS.hours, BUSINESS.location and BUSINESS.phone, sized to its content rather than h-dvh, with no nested-scroll ambiguity."
  artifacts:
    - path: "src/app/(public)/layout.tsx"
      provides: "Chrome-free passthrough layout for the public route group"
      contains: "children"
    - path: "src/app/(public)/page.tsx"
      provides: "Home route that owns the viewport, overlays TopNav, and ends with the Footer as a fourth snap stop"
      contains: "TopNav"
    - path: "src/app/(public)/about/page.tsx"
      provides: "About page composing its own TopNav + Footer in normal flow"
      contains: "Footer"
    - path: "src/app/(public)/contact/page.tsx"
      provides: "Contact page composing its own TopNav + Footer in normal flow"
      contains: "Footer"
  key_links:
    - from: "src/app/(public)/page.tsx"
      to: "src/components/layout/TopNav.tsx"
      via: "overlay-positioned TopNav rendered as sibling of the snap container"
      pattern: "<TopNav"
    - from: "src/app/(public)/page.tsx"
      to: "scrollRef"
      via: "ref on the h-dvh overflow-y-auto snap container, passed to all three sections"
      pattern: "ref=\\{scrollRef\\}"
---

<objective>
Eliminate the double scrollbar on the home route by making the snap container the only
scroller on that page, while leaving `/about` and `/contact` as ordinary scrolling
documents with TopNav and Footer in normal flow.

Purpose: The home page currently renders a `100dvh` snap container *inside* a
`min-h-screen` flex column that also contains a `~64px` sticky nav and a tall footer.
Document height therefore exceeds the viewport, so the body gets a permanent outer
scrollbar in addition to the snap container's own. The wheel drives the inner one, so
the outer scrollbar is dead weight and the page never fits.

Output: Layout-only changes to the public route group. No estimate, VIN, pricing, or
Route Handler code is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

@src/app/(public)/layout.tsx
@src/app/(public)/page.tsx
@src/components/layout/TopNav.tsx
@src/components/layout/Footer.tsx
@src/components/home/ServicesSection.tsx
</context>

<approach>
## Locked decision being implemented

"Home owns the viewport; nav overlays." The snap container in `page.tsx` becomes the only
scroller on `/`. `TopNav` is drawn on top of the sections instead of consuming vertical
space. Footer leaves the home route's normal flow.

## Chosen mechanism: move chrome composition down into the pages

`(public)/layout.tsx` becomes a passthrough (renders `{children}` only). Each of the three
public pages composes the chrome it needs:

- `/` renders `<TopNav />` as an absolutely-positioned overlay plus the snap container.
- `/about` and `/contact` each render the exact `flex min-h-screen flex-col` +
  `TopNav` + `main.flex-1` + `Footer` structure that the shared layout provides today.

**Why this over the alternatives:**

- **`usePathname()` conditional in the layout** — would force `(public)/layout.tsx` to
  become a Client Component. That pushes the whole public route group client-side and
  makes `/about` and `/contact` (currently server-rendered) lose static rendering. Too
  much blast radius for a CSS-height bug.
- **A new nested route group** (e.g. `(marketing)/`) — requires moving `about/` and
  `contact/` directories on disk. Larger diff, more chance of breaking imports, and the
  route group nesting buys nothing that per-page composition doesn't.
- **Per-page composition** — three small, explicit files. Each page states its own chrome,
  so there is no hidden coupling between `/` and the other two routes. The duplication is
  ~8 lines repeated twice, which is cheaper than the correctness risk of the other options.

## Footer treatment on home: its own fourth snap section

**USER DECISION (2026-08-05) — locked.** Two options were rejected before this one:

**Rejected: nesting the Footer inside `ServicesSection`.** `ServicesSection` is already
`h-dvh overflow-y-auto bg-background flex items-center` with a 4-card grid + 3 testimonials
+ a CTA inside — it already has its own internal scroller on short viewports. Appending the
Footer there would push content past `h-dvh` and force users to scroll *within* a snap
section, making the wheel ambiguous between advancing the snap and scrolling inner overflow.
That fights `snap-mandatory`. This objection stands and is why the Footer does NOT go here.

**Rejected: omitting the Footer from `/`.** The Footer carries `BUSINESS.hours` (Business
Hours), `BUSINESS.location`, `BUSINESS.serviceArea`, and `BUSINESS.phone`. For a local
service business landing page that is primary content and locally SEO-relevant, not chrome —
a homepage visitor looking for hours should not have to navigate to `/about`. Dropping it is
a product decision, not a layout fix, and the user declined it.

**Chosen: the Footer becomes a fourth snap stop** inside the snap container, after
`ServicesSection`, carrying `snap-start snap-always` like its three siblings.

**It must NOT be `h-dvh`.** `src/components/layout/Footer.tsx` is a compact 3-column grid
(brand/service-area, hours, contact) plus a copyright bar at `py-8 md:py-12`. Stretched to a
full viewport it would read as sparse and broken. Size the stop to its content while keeping
it a clean snap target — no partial section left visible above it when snapped. The executor
must choose and justify the exact treatment (e.g. the `<Footer />` itself as the snap target
with `snap-start snap-always` on a content-height wrapper). This is the one genuinely open
detail in this plan — verify it visually rather than assuming.

`src/components/layout/Footer.tsx` itself should need no modification. If it does, keep the
change minimal and ensure `/about` and `/contact` still render it identically to today.

## Nav overlay mechanics

`TopNav`'s root `<nav>` currently carries `sticky top-0 z-50`. `sticky` is what makes it
consume a row in the flex column. To keep `/about` and `/contact` behavior identical while
letting `/` overlay it, `TopNav` takes an optional `overlay?: boolean` prop:

- `overlay` unset/false (the `/about`, `/contact` case) → `sticky top-0` exactly as today.
- `overlay` true (the `/` case) → `absolute top-0` so it is removed from flow and painted
  over the hero. `z-50` and the existing
  `bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60` classes stay
  in both modes, so it remains legible over the hero photo.

`absolute` (not `fixed`): the nav is positioned against the home page's `relative` wrapper,
which is exactly one viewport tall and is not itself scrolled. `fixed` would work visually
too, but `absolute` keeps the nav out of the scroll container entirely and avoids any
interaction with the snap container's scroll position on mobile browsers where `fixed`
elements shift with the visual viewport.

Click-through is bounded by the nav's own box: the root `<nav>` gets `w-full` and its inner
`div` is `h-16`. Nothing below the 64px band is covered, because the `<nav>` has no height
beyond its content and no `inset-0`. Do not add `h-dvh`, `inset-0`, or a full-height
wrapper around it.
</approach>

<tasks>

<task type="auto">
  <name>Task 1: Make the public layout a passthrough and give /about and /contact their own chrome</name>

  <files>
src/app/(public)/layout.tsx
src/app/(public)/about/page.tsx
src/app/(public)/contact/page.tsx
  </files>

  <read_first>
- `src/app/(public)/layout.tsx` (all 16 lines) — the current shared chrome being relocated.
- `src/app/(public)/about/page.tsx` — read the full file to find the current outermost
  returned element, so the chrome wraps it without altering inner markup.
- `src/app/(public)/contact/page.tsx` — same.
- `src/components/layout/TopNav.tsx` lines 19-25 — confirm `TopNav` takes no props today.
- `src/components/layout/Footer.tsx` lines 1-5 — confirm `Footer` takes no props and is a
  Server Component (no `'use client'`).
  </read_first>

  <action>
Rewrite `src/app/(public)/layout.tsx` so `PublicLayout` returns `children` directly with no
wrapper element and no `TopNav`/`Footer` imports. Keep it a Server Component — do not add
`'use client'`. Keep the same default-export function name and the same
`{ children }: { children: React.ReactNode }` signature.

In `src/app/(public)/about/page.tsx` and `src/app/(public)/contact/page.tsx`, add the chrome
that the layout used to supply. Import `TopNav` from `@/components/layout/TopNav` and
`Footer` from `@/components/layout/Footer` using the existing `@/` alias style already used
in these files. Wrap each page's existing returned tree in the exact structure the layout
used: an outer `div` with className `flex min-h-screen flex-col`, then `<TopNav />`, then a
`main` with className `flex-1` containing the page's previous root element unchanged, then
`<Footer />`. Do not pass an `overlay` prop here — these routes keep the sticky nav.

Do not modify any inner markup, copy, data fetching, metadata exports, or `'use client'`
directives already present in these two page files. If a page is currently a Server
Component, it stays one. If either page already renders its own `TopNav` or `Footer`, do not
duplicate it — leave that page's chrome as-is and note it in the summary.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx eslint src --ext .ts,.tsx</automated>
  </verify>

  <acceptance_criteria>
- `grep -c 'TopNav\|Footer' "src/app/(public)/layout.tsx"` returns 0 — the layout no longer
  references either component.
- `src/app/(public)/layout.tsx` contains no `min-h-screen` and no `<main` element.
- `src/app/(public)/layout.tsx` does NOT contain `'use client'`.
- `src/app/(public)/about/page.tsx` contains all four of: `flex min-h-screen flex-col`,
  `<TopNav />`, `className="flex-1"`, `<Footer />`.
- `src/app/(public)/contact/page.tsx` contains the same four strings.
- Neither `about/page.tsx` nor `contact/page.tsx` contains the substring `overlay`.
- `npx tsc --noEmit` exits 0.
- `npx eslint src --ext .ts,.tsx` exits 0.
  </acceptance_criteria>

  <done>
The public layout renders only its children. `/about` and `/contact` each compose
`TopNav` + `main.flex-1` + `Footer` inside a `flex min-h-screen flex-col` wrapper, matching
the previous shared-layout output byte-for-byte in structure. Type check and lint pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add overlay mode to TopNav and make the home snap container the only scroller</name>

  <files>
src/components/layout/TopNav.tsx
src/app/(public)/page.tsx
  </files>

  <read_first>
- `src/components/layout/TopNav.tsx` lines 16-25 — the `cn` import is already present at
  line 16 and the root `<nav>` className is at line 24. Reuse `cn`; do not add a new
  class-merging helper.
- `src/app/(public)/page.tsx` (all 21 lines) — the `scrollRef` declaration and the snap
  container it is attached to.
- `src/components/home/HeroSection.tsx` lines 13-19 — `scrollToEstimate` reads
  `scrollRef.current` and calls `container.scrollTo({ top: container.clientHeight })`. The
  ref target must stay the element whose `clientHeight` equals one section height.
- `src/components/home/EstimateSection.tsx` line 156 — `viewport={{ root: scrollRef, ... }}`.
- `src/components/home/ServicesSection.tsx` lines 62, 88, 113 — three more
  `viewport={{ root: scrollRef, ... }}` usages.
  </read_first>

  <action>
In `src/components/layout/TopNav.tsx`, add an optional `overlay?: boolean` prop to the
`TopNav` component via a small exported-or-local props interface, defaulting to `false`.
Replace the hardcoded `sticky top-0` in the root `<nav>` className with a `cn()` call that
selects `absolute top-0` when `overlay` is true and `sticky top-0` when it is false. Every
other class on that element stays exactly as-is and applies in both modes — specifically
`z-50`, `w-full`, `border-b`, `border-border`, `bg-background/95`, `backdrop-blur`, and
`supports-[backdrop-filter]:bg-background/60`. Do not touch the inner `div`'s
`container mx-auto flex h-16 items-center justify-between px-4`, the nav links, the phone
link, or the mobile Sheet. Do not add `inset-0`, `h-dvh`, `fixed`, or `pointer-events-none`.

In `src/app/(public)/page.tsx`, import `TopNav` from `@/components/layout/TopNav`. Wrap the
existing return in an outer `div` with className `relative h-dvh overflow-hidden`, and inside
it render `<TopNav overlay />` followed by the existing snap container div, unchanged. The
snap container keeps `ref={scrollRef}` and keeps its exact className
`h-dvh overflow-y-auto snap-y snap-mandatory`, and keeps passing `scrollRef` to all three
sections. `scrollRef` must remain attached to that same snap div — it is the
IntersectionObserver root for four `whileInView` animations and the target of the hero's
`scrollTo`. Do not add `snap-y`, `snap-mandatory`, or `overflow` classes to `<body>`,
`<html>`, or the new outer wrapper other than the `overflow-hidden` specified above.

Also import `Footer` from `@/components/layout/Footer` and render it as the FOURTH and last
child *inside* the snap container, after `<ServicesSection />`, as its own snap stop carrying
`snap-start snap-always`.

Sizing: do NOT give the Footer stop `h-dvh`. `Footer` is a compact 3-column grid plus a
copyright bar (`py-8 md:py-12`) and would look sparse stretched to a full viewport. Size the
stop to the Footer's own content height. It must still snap cleanly — when snapped to it, no
partial slice of `ServicesSection` should remain visible above it, and snapping back up to
`ServicesSection` must land cleanly. Pick the treatment that achieves this (a content-height
wrapper carrying the snap classes is the expected shape) and record what you chose and why in
SUMMARY.md. Verify this visually at the checkpoint — it is the one detail this plan
deliberately leaves to your judgment.

Do NOT pass `scrollRef` to the Footer — it takes no props. The three `scrollRef={scrollRef}`
call sites stay exactly three.

The outer wrapper's `overflow-hidden` is what kills the second scrollbar: the wrapper is
exactly `h-dvh`, the nav is `absolute` so it adds no height, and any residual sub-pixel
overflow is clipped rather than promoted to a body scrollbar.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx eslint src --ext .ts,.tsx</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
  </verify>

  <acceptance_criteria>
- `src/components/layout/TopNav.tsx` contains both the literal `absolute top-0` and the
  literal `sticky top-0`, and contains `overlay`.
- `src/components/layout/TopNav.tsx` root `<nav>` still contains `z-50`, `w-full`,
  `bg-background/95`, `backdrop-blur`, and `supports-[backdrop-filter]:bg-background/60`.
- `src/components/layout/TopNav.tsx` contains no `inset-0`, no `h-dvh`, and no `fixed`.
- `src/app/(public)/page.tsx` contains `Footer` and the Footer stop carries `snap-start` and
  `snap-always`.
- The Footer stop in `src/app/(public)/page.tsx` does NOT carry `h-dvh` —
  `grep -n 'h-dvh' "src/app/(public)/page.tsx"` shows `h-dvh` only on the outer wrapper and
  the snap container (2 occurrences), not on the Footer stop.
- `grep -c 'scrollRef={scrollRef}' "src/app/(public)/page.tsx"` returns exactly 3 — the
  Footer takes no `scrollRef`.
- Behavior on `/` at desktop width: snapping down past `ServicesSection` lands on the Footer
  stop; it renders Business Hours (`BUSINESS.hours`), the location (`BUSINESS.location`),
  the service area (`BUSINESS.serviceArea`), and the phone (`BUSINESS.phone`). Snapping back
  up lands cleanly on `ServicesSection`. Neither direction leaves a partial section visible.
- Behavior on `/`: the wheel over the Footer stop advances/returns the snap — it does not
  scroll an inner overflow region.
- Behavior on `/` at 375x667: the Footer stop fits without the page scrolling inside the
  section, and its grid renders as a single column (`grid-cols-1 md:grid-cols-3`).
- Behavior on `/`: still exactly ONE vertical scrollbar and NO horizontal scrollbar, at both
  desktop and 375x667.
- `src/app/(public)/page.tsx` contains `relative h-dvh overflow-hidden` and `<TopNav overlay`.
- `src/app/(public)/page.tsx` contains `ref={scrollRef}` and the exact string
  `h-dvh overflow-y-auto snap-y snap-mandatory`.
- `src/app/(public)/page.tsx` contains no `Footer`.
- `grep -c 'scrollRef={scrollRef}' "src/app/(public)/page.tsx"` returns 3 — all three
  sections still receive the ref.
- No file under `src/` gained `snap-y` or `snap-mandatory` on `body` or `html`:
  `grep -rn 'snap-y\|snap-mandatory' src/app/globals.css` returns nothing.
- `npx tsc --noEmit` exits 0.
- `npx eslint src --ext .ts,.tsx` exits 0.
- `npm test` passes 33/33.
- `npm run build` exits 0.
  </acceptance_criteria>

  <done>
`TopNav` renders sticky by default and absolute when `overlay` is passed. The home page
wraps an overflow-clipped, one-viewport-tall relative container around an overlaid nav and
the unchanged snap container. `scrollRef` is still the snap container. Type check, lint,
33/33 tests, and production build all pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Layout-only fix for the double scrollbar on the home route. `(public)/layout.tsx` is now a
passthrough; `/about` and `/contact` compose their own `TopNav` + `Footer` in normal flow;
`/` wraps an `absolute`-positioned `TopNav` overlay plus the existing snap container in a
`relative h-dvh overflow-hidden` shell. On `/` the Footer is a fourth snap stop inside the
snap container, sized to its content rather than `h-dvh` (rationale in the plan's approach
section). `tsc`, `eslint src`, `npm test` (33/33), and `npm run build` all pass.
  </what-built>

  <how-to-verify>
Dev server runs on port **3001** — port 3000 is a stale process returning 500. Start it
with `npm run dev -- -p 3001` if it is not already up.

1. Open http://localhost:3001/ in a desktop browser at a normal window size.
   - Expected: exactly ONE vertical scrollbar. There is no second, taller scrollbar at the
     far right edge of the window.
2. Scroll with the mouse wheel on `/`.
   - Expected: the page snaps cleanly between the hero, the estimate card, and the
     services section — same feel as before. Each section fills the viewport.
3. Look at the top of the hero on `/`.
   - Expected: the nav bar is drawn over the hero photo, legible against it (translucent
     white / blurred background), with the logo, nav links, and phone number readable.
     The hero headline is not pushed down by a nav row.
4. Click a nav link and the phone number in the overlaid nav on `/`.
   - Expected: both respond. Then click the hero's "Get Free Estimate" button.
   - Expected: it smooth-scrolls to the estimate section (this exercises the `scrollRef`
     path that must not have broken).
5. Narrow the window to mobile width on `/` and open the hamburger menu.
   - Expected: the sheet opens over the sections and closes on link click.
6. Scroll to the third (services) section on `/` and let it settle.
   - Expected: the service cards and testimonials fade/slide in. If they appear instantly
     with no animation, the IntersectionObserver root broke — report it.
7. **Non-regression:** open http://localhost:3001/about.
   - Expected: nav bar sits ABOVE the content in normal flow (content starts below it, not
     underneath it), page scrolls as an ordinary document, and the Footer (business hours,
     service area, contact, copyright line) is visible at the bottom after scrolling.
8. **Non-regression:** open http://localhost:3001/contact and repeat check 7.
   - Expected: same — TopNav above content in flow, Footer at the bottom.
9. On `/`, confirm the Footer is absent and that the services section still ends with a
   "Contact Us" button linking to `/contact`.
  </how-to-verify>

  <resume-signal>Type "approved" or describe which numbered check failed and what you saw.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan changes only client-side layout markup and Tailwind class strings. No new input parsing, no network calls, no auth surface, no data persistence, no dependency installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-i19-01 | Tampering | `TopNav` overlay z-index/positioning | accept | An `absolute z-50` nav could in principle overlay interactive content (clickjacking-adjacent). Bounded here: the nav is same-origin first-party markup, has no `inset-0`, and covers only its own 64px band. No third-party frame is involved. |
| T-i19-02 | Denial of Service | `overflow-hidden` on the home wrapper | mitigate | Clipping could hide content if a section overflows one viewport. Checkpoint steps 2, 3, and 6 confirm every section still fits and is reachable via the snap container. |
| T-i19-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages. No `package.json` change. If the executor finds itself needing an install, stop and escalate rather than adding a dependency. |
</threat_model>

<verification>
Run from the repo root. All four must pass:

```
npx tsc --noEmit
npx eslint src --ext .ts,.tsx
npm test
npm run build
```

`npm test` must report 33/33 passing (the Phase 3 pricing + VIN suites — unchanged by this
plan; any change in that count means something outside the layout was touched).

Do NOT run repo-wide `npm run lint` — it reports ~17k false problems from stale build output
under `.claude/worktrees/`. Scope lint to `src` as shown.

Source assertions (single grep pass each):

```
grep -c 'TopNav\|Footer' "src/app/(public)/layout.tsx"          # -> 0
grep -c 'scrollRef={scrollRef}' "src/app/(public)/page.tsx"     # -> 3
grep -c 'Footer' "src/app/(public)/page.tsx"                    # -> 0
grep -c 'Footer' "src/app/(public)/about/page.tsx"              # -> >= 1
grep -c 'Footer' "src/app/(public)/contact/page.tsx"            # -> >= 1
grep -n 'snap-y\|snap-mandatory' src/app/globals.css            # -> no output
```

Then the blocking human-verify checkpoint above.
</verification>

<success_criteria>
- `/` renders exactly one vertical scrollbar, owned by the snap container.
- Wheel scrolling on `/` still snaps between three full-viewport sections.
- `scrollRef` is still attached to the `h-dvh overflow-y-auto snap-y snap-mandatory` div and
  is still passed to all three sections; hero scroll-to and all four `whileInView`
  animations still fire.
- `TopNav` overlays the hero on `/` without consuming vertical space, remains legible, and
  remains clickable; it does not cover content outside its own 64px band.
- `/about` and `/contact` render `TopNav` in normal flow above content and `Footer` in
  normal flow below it, and scroll as ordinary documents.
- Sections remain `h-dvh` with `snap-start snap-always` — no `calc()` height shrinking.
- No change to `src/lib/pricing.ts`, `src/app/api/**`, or any VIN/estimate logic.
- `npx tsc --noEmit`, `npx eslint src --ext .ts,.tsx`, `npm test` (33/33), and
  `npm run build` all exit 0.
</success_criteria>

<output>
Create `.planning/quick/260805-i19-fix-double-scrollbar-on-snap-scroll-home/260805-i19-SUMMARY.md` when done.
</output>
