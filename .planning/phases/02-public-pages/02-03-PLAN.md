---
phase: 02-public-pages
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/(public)/page.tsx
  - src/components/home/ServicesSection.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The third snap section displays 3-4 service cards in a responsive grid"
    - "Each service card has a lucide-react icon, title, and description"
    - "2-3 testimonial quotes appear below the service cards"
    - "A Contact Us CTA button links to /contact at the bottom"
    - "Section content animates in when scrolling into view"
  artifacts:
    - path: "src/components/home/ServicesSection.tsx"
      provides: "Service cards grid + testimonial quotes + Contact CTA"
      contains: "Windshield Repair"
    - path: "src/app/(public)/page.tsx"
      provides: "Updated snap container importing ServicesSection"
      contains: "ServicesSection"
  key_links:
    - from: "src/app/(public)/page.tsx"
      to: "src/components/home/ServicesSection.tsx"
      via: "import and scrollRef prop"
      pattern: "ServicesSection.*scrollRef"
    - from: "src/components/home/ServicesSection.tsx"
      to: "/contact"
      via: "Contact Us link"
      pattern: "href.*contact"
---

<objective>
Build the Services + Testimonials section (snap section 3) with a responsive service cards grid, testimonial quotes, and a final Contact Us CTA. Wire it into the snap scroll container replacing the placeholder.

Purpose: The third section answers "Can I trust you?" — showcasing services offered and social proof via testimonials. The Contact Us CTA closes the conversion funnel.

Output: Fully styled third snap section with service cards, testimonials, and CTA integrated into the home page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-public-pages/02-CONTEXT.md
@.planning/phases/02-public-pages/02-RESEARCH.md
@.planning/phases/02-public-pages/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — snap container + section pattern -->

From src/app/(public)/page.tsx (after Plan 01):
```typescript
// Section 3 is currently a placeholder <section> — replace with <ServicesSection scrollRef={scrollRef} />
// scrollRef type: RefObject<HTMLDivElement | null>
```

From src/components/home/HeroSection.tsx (section shell pattern):
```typescript
// Each section: <section className="snap-start snap-always h-dvh relative overflow-hidden ...">
// motion whileInView: viewport={{ root: scrollRef, once: true, amount: 0.3 }}
```

From src/components/ui/card.tsx:
```typescript
export { Card, CardHeader, CardFooter, CardTitle, CardAction, CardDescription, CardContent }
```

From src/components/ui/button.tsx:
```typescript
// Button default = brand red bg. Does NOT support asChild.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ServicesSection with service cards, testimonials, and Contact CTA</name>
  <files>src/components/home/ServicesSection.tsx</files>
  <read_first>
    - .planning/phases/02-public-pages/02-RESEARCH.md (Pattern 3: motion whileInView with viewport root)
    - .planning/phases/02-public-pages/02-CONTEXT.md (D-14, D-15, D-16)
    - src/components/home/HeroSection.tsx (section shell pattern — snap-start snap-always h-dvh)
    - src/components/ui/card.tsx (Card component API)
    - src/components/ui/button.tsx (Button component API)
  </read_first>
  <action>
Create `src/components/home/ServicesSection.tsx` implementing snap section 3 (per D-14, D-15, D-16):

```tsx
'use client'

import { type RefObject } from 'react'
import Link from 'next/link'
import { motion } from 'motion/react'
import { Wrench, Replace, ScanLine, Car } from 'lucide-react'
import { Card, CardContent } from '@/components/ui/card'
import { Button } from '@/components/ui/button'

interface ServicesSectionProps {
  scrollRef: RefObject<HTMLDivElement | null>
}

const SERVICES = [
  {
    icon: Wrench,
    title: 'Windshield Repair',
    description: 'Quick chip and crack repair that restores structural integrity. Most repairs completed in under 30 minutes.',
  },
  {
    icon: Replace,
    title: 'Full Replacement',
    description: 'Complete windshield replacement using OEM-quality glass. We handle all vehicle makes and models.',
  },
  {
    icon: ScanLine,
    title: 'ADAS Calibration',
    description: 'Advanced driver assistance system recalibration after windshield replacement for 2018+ vehicles.',
  },
  {
    icon: Car,
    title: 'Mobile Service',
    description: 'We come to you anywhere in San Antonio. Same-day mobile service at your home or workplace.',
  },
] as const

const TESTIMONIALS = [
  {
    name: 'Maria G.',
    quote: 'They came to my office and replaced my windshield during my lunch break. Professional and fast. Highly recommend!',
  },
  {
    name: 'James R.',
    quote: 'Best price I found in San Antonio. The estimate tool made it easy to know what to expect before I even called.',
  },
  {
    name: 'Carlos M.',
    quote: 'Lifetime warranty on the installation gave me peace of mind. Great communication throughout the whole process.',
  },
] as const

export function ServicesSection({ scrollRef }: ServicesSectionProps) {
  return (
    <section className="snap-start snap-always h-dvh overflow-y-auto bg-background flex items-center">
      <div className="w-full py-12 px-4">
        <div className="max-w-6xl mx-auto">
          {/* Service Cards (per D-14) */}
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6, ease: 'easeOut' }}
            viewport={{ root: scrollRef, once: true, amount: 0.2 }}
          >
            <h2 className="font-display text-3xl font-bold text-center text-foreground mb-8">
              Our Services
            </h2>
            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
              {SERVICES.map((service) => {
                const Icon = service.icon
                return (
                  <Card key={service.title} className="text-center">
                    <CardContent className="pt-6">
                      <Icon className="h-10 w-10 text-primary mx-auto mb-3" />
                      <h3 className="font-semibold text-foreground mb-1">{service.title}</h3>
                      <p className="text-sm text-muted-foreground">{service.description}</p>
                    </CardContent>
                  </Card>
                )
              })}
            </div>
          </motion.div>

          {/* Testimonials (per D-15) */}
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6, ease: 'easeOut', delay: 0.2 }}
            viewport={{ root: scrollRef, once: true, amount: 0.2 }}
            className="mt-10"
          >
            <h2 className="font-display text-2xl font-bold text-center text-foreground mb-6">
              What Our Customers Say
            </h2>
            <div className="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-4xl mx-auto">
              {TESTIMONIALS.map((t) => (
                <div key={t.name} className="text-center">
                  <p className="text-muted-foreground italic text-sm leading-relaxed">
                    &ldquo;{t.quote}&rdquo;
                  </p>
                  <p className="mt-2 font-semibold text-foreground text-sm">
                    &mdash; {t.name}
                  </p>
                </div>
              ))}
            </div>
          </motion.div>

          {/* Contact CTA (per D-16) */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.5, ease: 'easeOut', delay: 0.3 }}
            viewport={{ root: scrollRef, once: true, amount: 0.5 }}
            className="mt-8 text-center"
          >
            <Link href="/contact">
              <Button size="lg" className="text-base px-8 py-3 h-auto">
                Contact Us
              </Button>
            </Link>
          </motion.div>
        </div>
      </div>
    </section>
  )
}
```

Key implementation details:
- **D-14**: 4 service cards (Windshield Repair, Full Replacement, ADAS Calibration, Mobile Service) in a responsive grid: 1 col on mobile, 2 on `sm`, 4 on `lg`. Each card uses shadcn `Card` + `CardContent` with a lucide-react icon (`Wrench`, `Replace`, `ScanLine`, `Car`), title, and 1-2 sentence description.
- **D-15**: 3 placeholder testimonial quotes with fictional names. Simple format: curly quotes + name. No rating stars (per D-15).
- **D-16**: "Contact Us" button at the bottom links to `/contact`. Uses default Button variant (brand red).
- **D-08**: All three content blocks use `motion.div` with `whileInView` and `viewport={{ root: scrollRef, once: true }}` — CRITICAL for snap container compatibility.
- Section uses `bg-background` (white) — no background photo on section 3 (per D-06, only sections 1 and 2 have background photos).
- Section has `overflow-y-auto` in addition to `h-dvh` so content is scrollable within the snap section if the viewport is very small. The `flex items-center` vertically centers content in normal viewports.
- Icon choices: `Wrench` (repair), `Replace` (replacement — if `Replace` doesn't exist in lucide-react, use `RefreshCcw` or `Layers`), `ScanLine` (ADAS calibration — scanning sensors), `Car` (mobile service).

**Lucide icon note:** Verify that `Replace`, `ScanLine` are available in the installed `lucide-react@1.8.0`. If not available, use these alternatives:
- `Replace` -> `ArrowLeftRight` or `RefreshCcw`
- `ScanLine` -> `Radar` or `Focus`
- `Car` -> `CarFront` or `Truck`
  </action>
  <verify>
    <automated>cd F:/2026/Projects/Windshield && test -f src/components/home/ServicesSection.tsx && grep -q "Windshield Repair" src/components/home/ServicesSection.tsx && grep -q "Full Replacement" src/components/home/ServicesSection.tsx && grep -q "ADAS Calibration" src/components/home/ServicesSection.tsx && grep -q "Mobile Service" src/components/home/ServicesSection.tsx && grep -q "whileInView" src/components/home/ServicesSection.tsx && grep -q "viewport.*root.*scrollRef" src/components/home/ServicesSection.tsx && grep -q "Contact Us" src/components/home/ServicesSection.tsx && grep -q 'href="/contact"' src/components/home/ServicesSection.tsx && grep -q "TESTIMONIALS" src/components/home/ServicesSection.tsx && grep -q "h-dvh" src/components/home/ServicesSection.tsx && echo "PASS: ServicesSection checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/home/ServicesSection.tsx` exists with `'use client'` directive
    - Contains 4 service cards: "Windshield Repair", "Full Replacement", "ADAS Calibration", "Mobile Service"
    - Each card has a lucide-react icon imported at the top
    - Contains `TESTIMONIALS` array with 3 entries (placeholder names and quotes)
    - Testimonial quotes use curly quote marks, no rating stars
    - "Contact Us" button links to `/contact` (per D-16)
    - Uses `whileInView` with `viewport={{ root: scrollRef, once: true }}` on all motion elements
    - Section has `h-dvh` class (not h-screen)
    - Responsive grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`
    - Imports from `motion/react` (not framer-motion)
  </acceptance_criteria>
  <done>ServicesSection renders 4 service cards in a responsive grid, 3 testimonial quotes, and a Contact Us CTA — all with whileInView animations using viewport root</done>
</task>

<task type="auto">
  <name>Task 2: Wire ServicesSection into page.tsx snap container</name>
  <files>src/app/(public)/page.tsx</files>
  <read_first>
    - src/app/(public)/page.tsx (current state — has placeholder section 3 and EstimateSection or placeholder for section 2)
    - src/components/home/ServicesSection.tsx (just created — confirm export name)
  </read_first>
  <action>
Update `src/app/(public)/page.tsx` to replace the section 3 placeholder with the real ServicesSection component:

1. Add import: `import { ServicesSection } from '@/components/home/ServicesSection'`
2. Replace the placeholder `<section>` for section 3 with: `<ServicesSection scrollRef={scrollRef} />`

**Important:** This plan runs in Wave 2 parallel with Plan 02. Both plans modify `page.tsx`. The executor should read the CURRENT state of `page.tsx` at execution time — it may or may not already have `EstimateSection` wired in (depending on whether Plan 02 ran first). The executor should:
- If Plan 02 already ran: page.tsx has `EstimateSection` imported and rendered. Add `ServicesSection` import and replace the section 3 placeholder only.
- If Plan 02 has NOT run yet: page.tsx still has both placeholder sections. Replace only the section 3 placeholder with `ServicesSection`. Leave the section 2 placeholder for Plan 02.

The final state of page.tsx (after both Plans 02 and 03) should be:
```tsx
'use client'

import { useRef } from 'react'
import { HeroSection } from '@/components/home/HeroSection'
import { EstimateSection } from '@/components/home/EstimateSection'
import { ServicesSection } from '@/components/home/ServicesSection'

export default function HomePage() {
  const scrollRef = useRef<HTMLDivElement>(null)

  return (
    <div
      ref={scrollRef}
      className="h-dvh overflow-y-auto snap-y snap-mandatory"
    >
      <HeroSection scrollRef={scrollRef} />
      <EstimateSection scrollRef={scrollRef} />
      <ServicesSection scrollRef={scrollRef} />
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd F:/2026/Projects/Windshield && grep -q "ServicesSection" src/app/\(public\)/page.tsx && grep -q "import.*ServicesSection" src/app/\(public\)/page.tsx && npx next build 2>&1 | tail -5 && echo "PASS: page.tsx wired with ServicesSection and build passes"</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` imports `ServicesSection` from `@/components/home/ServicesSection`
    - `page.tsx` renders `<ServicesSection scrollRef={scrollRef} />` in position 3
    - `next build` succeeds
  </acceptance_criteria>
  <done>ServicesSection wired into the snap container as section 3, build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | No user input, no API calls, no data persistence in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | Testimonial content | accept | Placeholder text only; no real customer data. Owner replaces before launch. |
</threat_model>

<verification>
1. Scroll to section 3 — service cards grid visible with icons
2. 4 service cards render: Windshield Repair, Full Replacement, ADAS Calibration, Mobile Service
3. Cards are responsive: 1 col mobile, 2 col tablet, 4 col desktop
4. 3 testimonial quotes visible below the cards
5. "Contact Us" button navigates to /contact
6. whileInView animations play when section scrolls into view
7. `next build` succeeds
</verification>

<success_criteria>
- Third snap section displays service cards and testimonials (implements D-14, D-15, D-16)
- Service cards have icons, titles, and descriptions in responsive grid (D-14)
- Testimonial quotes are placeholder text with names, no stars (D-15)
- Contact Us CTA links to /contact (D-16)
- Animations fire via whileInView with viewport root
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-pages/02-03-SUMMARY.md`
</output>
