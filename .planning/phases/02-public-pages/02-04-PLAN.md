---
phase: 02-public-pages
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(public)/about/page.tsx
  - src/components/about/MissionSection.tsx
  - src/components/about/VisionSection.tsx
  - src/components/about/TrustSection.tsx
autonomous: true
requirements:
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03
  - ABOUT-04
must_haves:
  truths:
    - "The About page displays a company mission statement in a white-background section"
    - "A vision statement appears in a light-gray background section"
    - "Trust signals show lifetime warranty, San Antonio service area, and insurance mention"
    - "MissionSection intro text states how long the business has been serving San Antonio (e.g., 'since 2020')"
    - "Sections alternate between white and light-gray backgrounds"
    - "No ADAS calibration callout appears on the About page"
  artifacts:
    - path: "src/app/(public)/about/page.tsx"
      provides: "About page composing three sections"
      contains: "MissionSection"
    - path: "src/components/about/MissionSection.tsx"
      provides: "Company mission statement section with years-in-business callout"
      contains: "Our Mission"
    - path: "src/components/about/VisionSection.tsx"
      provides: "Company vision statement section"
      contains: "Our Vision"
    - path: "src/components/about/TrustSection.tsx"
      provides: "Trust signals with warranty, service area, insurance"
      contains: "Lifetime Warranty"
  key_links:
    - from: "src/app/(public)/about/page.tsx"
      to: "src/components/about/MissionSection.tsx"
      via: "import"
      pattern: "MissionSection"
    - from: "src/components/about/TrustSection.tsx"
      to: "src/lib/constants.ts"
      via: "BUSINESS constant for service area"
      pattern: "BUSINESS"
---

<objective>
Build the full About page with alternating white/light-gray sections for mission, vision, and trust signals. The About page uses standard document flow (NOT snap scroll).

Purpose: The About page builds credibility and trust — answering "Who are they?" and "Can I trust them?" Content is placeholder text for v1; the structure and layout are the deliverables.

Output: A visually complete About page with three sections, trust signals including warranty and service area, and responsive layout. The mission section includes years-in-business text (ABOUT-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-public-pages/02-CONTEXT.md
@.planning/phases/02-public-pages/02-RESEARCH.md

<interfaces>
<!-- Existing components the executor needs -->

From src/app/(public)/layout.tsx:
```typescript
// Structure: <div flex min-h-screen flex-col> <TopNav/> <main flex-1>{children}</main> <Footer/> </div>
// About page content renders inside <main> with normal document flow (NO snap scroll)
```

From src/lib/constants.ts:
```typescript
export const BUSINESS = {
  name: 'Alamo City Windshield Repair',
  phone: '(210) 555-0100',
  phoneHref: 'tel:+12105550100',
  location: 'San Antonio, TX',
  serviceArea: 'Mobile service available across San Antonio',
  hours: [...],
} as const
```

From src/app/globals.css:
```css
--background: oklch(1 0 0);         /* white — use bg-background */
--muted: oklch(0.97 0 0);           /* light gray — use bg-muted for alternating sections */
--primary: oklch(0.505 0.213 27.518); /* brand red */
--font-display: var(--font-space-grotesk); /* for headings */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create About page sections (Mission, Vision, Trust) and compose in page.tsx</name>
  <files>src/app/(public)/about/page.tsx, src/components/about/MissionSection.tsx, src/components/about/VisionSection.tsx, src/components/about/TrustSection.tsx</files>
  <read_first>
    - .planning/phases/02-public-pages/02-CONTEXT.md (D-17, D-18, D-19, D-20)
    - .planning/phases/02-public-pages/02-RESEARCH.md (Pattern 7: About Page Alternating Sections)
    - src/app/(public)/about/page.tsx (current placeholder to replace)
    - src/app/globals.css (confirm --muted token value for bg-muted)
    - src/lib/constants.ts (BUSINESS constant for service area info)
  </read_first>
  <action>
Create three section components and rewrite the About page (per D-17, D-18, D-19, D-20):

**File 1: `src/components/about/MissionSection.tsx`** (per D-17 section 1, ABOUT-01, ABOUT-03)

```tsx
import { BUSINESS } from '@/lib/constants'

export function MissionSection() {
  return (
    <section className="bg-background py-16 lg:py-24">
      <div className="container mx-auto px-4 max-w-4xl">
        <h2 className="font-display text-3xl font-bold text-foreground md:text-4xl">
          Our Mission
        </h2>
        <div className="mt-6 space-y-4 text-muted-foreground leading-relaxed">
          <p>
            Serving San Antonio since 2020, {BUSINESS.name} is dedicated to providing
            every driver with fast, affordable, and high-quality windshield repair and
            replacement services. We believe that a damaged windshield shouldn&apos;t
            disrupt your day or drain your wallet.
          </p>
          <p>
            We&apos;re committed to making the process simple: get an instant estimate,
            book a time that works for you, and let our certified technicians handle the
            rest. Whether you come to us or we come to you, we treat every vehicle as if
            it were our own.
          </p>
          <p>
            Safety is at the core of everything we do. A properly installed windshield
            protects you and your passengers in a collision, and we never cut corners on
            materials or workmanship.
          </p>
        </div>
      </div>
    </section>
  )
}
```

**File 2: `src/components/about/VisionSection.tsx`** (per D-17 section 2, ABOUT-02)

```tsx
export function VisionSection() {
  return (
    <section className="bg-muted py-16 lg:py-24">
      <div className="container mx-auto px-4 max-w-4xl">
        <h2 className="font-display text-3xl font-bold text-foreground md:text-4xl">
          Our Vision
        </h2>
        <div className="mt-6 space-y-4 text-muted-foreground leading-relaxed">
          <p>
            We envision a San Antonio where no driver waits more than a day to get a
            windshield issue resolved. By combining technology with skilled craftsmanship,
            we&apos;re building the most transparent and accessible auto glass service in
            the city.
          </p>
          <p>
            Our online estimate tool is just the beginning. We&apos;re working toward a
            future where the entire experience — from diagnosis to repair — is faster,
            fairer, and more convenient than anything currently available.
          </p>
          <p>
            As San Antonio grows, so do we. Our goal is to be the first name that comes
            to mind when anyone in the city needs windshield service — earned through
            consistent quality, honest pricing, and genuine care for our customers.
          </p>
        </div>
      </div>
    </section>
  )
}
```

**File 3: `src/components/about/TrustSection.tsx`** (per D-17 section 3, D-18, ABOUT-03, ABOUT-04)

```tsx
import { ShieldCheck, MapPin, FileCheck } from 'lucide-react'
import { BUSINESS } from '@/lib/constants'

const TRUST_SIGNALS = [
  {
    icon: ShieldCheck,
    title: 'Lifetime Warranty on Installation',
    description:
      'Every windshield we install comes with a lifetime warranty against leaks and defects. Our work is guaranteed for as long as you own your vehicle.',
  },
  {
    icon: MapPin,
    title: 'Serving All of San Antonio',
    description:
      'From the Alamo to the Medical Center, Stone Oak to Southtown — we provide mobile service across the entire San Antonio metro area. We come to your home, office, or wherever is most convenient.',
    areas: [
      'Downtown & Alamo Heights',
      'North Side & Stone Oak',
      'Medical Center & UTSA',
      'Southtown & South Side',
      'Westside & Helotes',
      'East Side & Converse',
    ],
  },
  {
    icon: FileCheck,
    title: 'Insurance Friendly',
    description:
      'We work with all major insurance providers to make your claim process as smooth as possible. While we don\'t file claims directly, we provide all the documentation your insurer needs.',
  },
] as const

export function TrustSection() {
  return (
    <section className="bg-background py-16 lg:py-24">
      <div className="container mx-auto px-4 max-w-4xl">
        <h2 className="font-display text-3xl font-bold text-foreground md:text-4xl">
          Why Choose {BUSINESS.name}
        </h2>
        <div className="mt-10 space-y-12">
          {TRUST_SIGNALS.map((signal) => {
            const Icon = signal.icon
            return (
              <div key={signal.title} className="flex gap-4">
                <div className="shrink-0">
                  <Icon className="h-8 w-8 text-primary" />
                </div>
                <div>
                  <h3 className="text-lg font-semibold text-foreground">
                    {signal.title}
                  </h3>
                  <p className="mt-1 text-muted-foreground leading-relaxed">
                    {signal.description}
                  </p>
                  {'areas' in signal && (
                    <ul className="mt-3 grid grid-cols-1 sm:grid-cols-2 gap-x-8 gap-y-1 text-sm text-muted-foreground">
                      {signal.areas.map((area) => (
                        <li key={area} className="flex items-center gap-2">
                          <span className="h-1.5 w-1.5 rounded-full bg-primary shrink-0" />
                          {area}
                        </li>
                      ))}
                    </ul>
                  )}
                </div>
              </div>
            )
          })}
        </div>
      </div>
    </section>
  )
}
```

**File 4: Rewrite `src/app/(public)/about/page.tsx`** (per ABOUT-01..04)

```tsx
import { MissionSection } from '@/components/about/MissionSection'
import { VisionSection } from '@/components/about/VisionSection'
import { TrustSection } from '@/components/about/TrustSection'

export default function AboutPage() {
  return (
    <>
      <MissionSection />
      <VisionSection />
      <TrustSection />
    </>
  )
}
```

Key implementation details:
- **D-17**: Three alternating full-width sections: Mission (white via `bg-background`), Vision (light-gray via `bg-muted`), Trust signals (white via `bg-background`).
- **ABOUT-03**: Years in business delivered in MissionSection first paragraph: "Serving San Antonio since 2020". This is placeholder text — the owner updates the year before launch.
- **D-18**: Trust signals include: (1) Lifetime warranty badge with `ShieldCheck` icon, (2) Service area "Serving All of San Antonio" with neighborhood list via `MapPin` icon, (3) Insurance mention "Insurance Friendly" with `FileCheck` icon. Service area list includes 6 SA neighborhoods in a 2-column grid.
- **D-19**: All content is professional placeholder text. Owner replaces before launch.
- **D-20**: No ADAS calibration callout on the About page (user explicitly chose not to include it).
- About page uses standard document flow (NO snap scroll) — it inherits the normal `flex-1` layout from `(public)/layout.tsx`.
- Section components are Server Components (no `'use client'`) — they have no state, no browser APIs, no motion animations. This is intentional: the About page is a content page, not an interactive experience.
- `BUSINESS.name` imported from constants for the mission section and trust section header.
- Headings use `font-display` (Space Grotesk) for consistency with the home page hero.
- `max-w-4xl` constrains content width for readability on large screens.
  </action>
  <verify>
    <automated>cd F:/2026/Projects/Windshield && test -f src/components/about/MissionSection.tsx && test -f src/components/about/VisionSection.tsx && test -f src/components/about/TrustSection.tsx && grep -q "Our Mission" src/components/about/MissionSection.tsx && grep -q "since" src/components/about/MissionSection.tsx && grep -q "bg-muted" src/components/about/VisionSection.tsx && grep -q "Lifetime Warranty" src/components/about/TrustSection.tsx && grep -q "Insurance" src/components/about/TrustSection.tsx && grep -q "Serving All of San Antonio" src/components/about/TrustSection.tsx && grep -q "MissionSection" src/app/\(public\)/about/page.tsx && grep -q "VisionSection" src/app/\(public\)/about/page.tsx && grep -q "TrustSection" src/app/\(public\)/about/page.tsx && grep -v "ADAS" src/components/about/TrustSection.tsx > /dev/null && npx next build 2>&1 | tail -5 && echo "PASS: About page checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/about/MissionSection.tsx` exists with "Our Mission" heading and `bg-background` (white)
    - MissionSection intro paragraph contains "since" to convey years in business (e.g., "Serving San Antonio since 2020") — placeholder the owner updates before launch (per ABOUT-03)
    - `src/components/about/VisionSection.tsx` exists with "Our Vision" heading and `bg-muted` (light gray)
    - `src/components/about/TrustSection.tsx` exists with "Lifetime Warranty", "Serving All of San Antonio", and "Insurance" content
    - TrustSection has `ShieldCheck`, `MapPin`, `FileCheck` icons from lucide-react
    - TrustSection does NOT contain "ADAS" (per D-20)
    - `src/app/(public)/about/page.tsx` imports and renders all three sections in order
    - About page does NOT use snap scroll or motion animations
    - Sections alternate `bg-background` / `bg-muted` / `bg-background` (per D-17)
    - Service area list includes San Antonio neighborhoods in a grid
    - All text is professional placeholder (per D-19)
    - `next build` succeeds
  </acceptance_criteria>
  <done>About page renders three alternating sections (Mission white, Vision gray, Trust white) with trust signals including warranty, service area with neighborhood list, and insurance mention. MissionSection includes "since 2020" years-in-business text (ABOUT-03). No ADAS callout. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Static content page with no user input, no API calls, no external resources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | Placeholder content | accept | All content is generic placeholder text. No sensitive business data exposed. |
</threat_model>

<verification>
1. Navigate to /about — three sections visible with alternating white/gray backgrounds
2. Mission section has "Our Mission" heading with "since 2020" (or similar) years-in-business text
3. Vision section has "Our Vision" heading with gray background
4. Trust section shows warranty badge, service area list, and insurance mention
5. No ADAS calibration content on the page
6. Page scrolls normally (no snap behavior)
7. Responsive layout on mobile, tablet, desktop
8. `next build` succeeds
</verification>

<success_criteria>
- About page shows company mission statement with years-in-business text (ABOUT-01, ABOUT-03)
- About page shows vision statement with light-gray background (ABOUT-02)
- About page shows service area, warranty, and insurance info (ABOUT-03, ABOUT-04)
- Trust signals include warranty badge, SA service area, insurance mention (ABOUT-04)
- Alternating section backgrounds (D-17)
- No ADAS calibration callout (D-20)
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-pages/02-04-SUMMARY.md`
</output>
