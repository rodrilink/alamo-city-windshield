---
phase: 06-analytics
plan: 05
type: execute
wave: 3
depends_on: ["06-02", "06-03", "06-04"]
files_modified:
  - .planning/phases/06-analytics/06-05-SUMMARY.md
autonomous: false
requirements: [ANLY-01, ANLY-02, ANLY-03, ANLY-04, ANLY-05, ANLY-06]

must_haves:
  truths:
    - "Each of the four event types has been observed landing as a real row in the analytics_events table"
    - "The ADMIN-02 visitors chart and ADMIN-04 VIN-search chart no longer render the Phase 5 empty state"
    - "The same VIN requested twice produces two vin_search rows — one from the fresh decode, one from the cache hit (D-15)"
    - "A honeypot-filled submission produces no new analytics row (D-11)"
    - "Every user-facing flow behaves exactly as it did before Phase 6 — no added error, no visible delay, no console noise in the browser"
  artifacts:
    - path: ".planning/phases/06-analytics/06-05-SUMMARY.md"
      provides: "Dated evidence that all four event types land and the two charts are populated"
  key_links:
    - from: "the four write sites"
      to: "the analytics_events table"
      via: "observed row counts before and after each exercised action"
      pattern: "row count delta"
    - from: "analytics_events"
      to: "the admin dashboard charts"
      via: "getVisitorSeries / getVinSearchSeries / getSummaryTotals reading the same event_type constants the writers emit"
      pattern: "chart renders non-empty"
---

<objective>
Prove end to end that all four event types land in `analytics_events` and that the two dashboard charts Phase 5 deliberately left empty now show real data.

Purpose: every check in plans 06-01 through 06-04 is static — typecheck, lint, grep, unit test. None of them can prove a row actually reaches Supabase, that the RLS policy admits the anonymous browser insert, or that the producer's strings match what the reader queries at runtime. The phase's headline risk (a producer/consumer mismatch rendering charts empty **without erroring**) is exactly the class of failure that static checks pass and reality fails. This plan is the runtime proof.

Per CONTEXT.md's discretion note: exercising each of the four events once and confirming the rows land — and that the charts stop showing the 05 D-01 empty state — is sufficient. A populated-*looking* chart is not required.

Output: `06-05-SUMMARY.md` with dated evidence per event type.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-analytics/06-CONTEXT.md
@.planning/phases/06-analytics/06-01-SUMMARY.md
@.planning/phases/06-analytics/06-02-SUMMARY.md
@.planning/phases/06-analytics/06-03-SUMMARY.md
@.planning/phases/06-analytics/06-04-SUMMARY.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Whole-repo static gate and write-site census</name>
  <files>(no source files modified — verification only)</files>
  <read_first>
    - `.planning/phases/06-analytics/06-01-SUMMARY.md` through `06-04-SUMMARY.md` — the four preceding plans' recorded call-site locations, so the census below can be checked against what each plan claims it did
    - `.planning/phases/06-analytics/06-CONTEXT.md` — D-01 (one source of truth), D-13 (one VIN insertion point, two branches), D-16 (no `/book` insertion point)
  </read_first>
  <action>
    Run the full static gate across the repository and record each command's exit code and output in the SUMMARY. Do not modify any source file in this task — if a check fails, stop and report the failure rather than patching it here, so the fix lands in the plan that owns the file.

    Run, in order: `npx tsc --noEmit`, `npx vitest run`, `npm run lint`, `npm run build`.

    Then produce a write-site census proving the event contract is intact and the call sites are exactly where the four plans placed them:
    - `grep -rn "'page_view'\|'vin_search'\|'contact_submit'\|'booking_created'" src/ --include=*.ts --include=*.tsx` — must match ONLY `src/lib/analytics/events.ts` and `src/lib/analytics/events.test.ts`. Any other file means D-01's single-source-of-truth property has already been broken.
    - `grep -rn "EVENT_TYPE_" src/` — must return nothing, proving the STATE.md blocker's private consts are gone.
    - `grep -rn "trackServerEvent(ANALYTICS_EVENTS\.\|trackBrowserEvent(ANALYTICS_EVENTS\." src/ --include=*.ts --include=*.tsx` — must return exactly five call sites: two `VIN_SEARCH` in `src/app/api/vin/[vin]/route.ts`, one `CONTACT_SUBMIT` in `src/lib/contact/contact-actions.ts`, one `BOOKING_CREATED` in `src/lib/booking/booking-actions.ts`, one `PAGE_VIEW` in `src/components/analytics/PageViewTracker.tsx`.
    - Confirm `src/app/(public)/book/page.tsx` contains no tracking call (D-16), and that no file under `src/app/(admin)/` references `PageViewTracker` (D-06).

    Record the census as a table in the SUMMARY. A count other than five, or a call site in an unexpected file, is a failure — report it rather than adjusting the expectation.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0 with zero failures and at least 12 test files
    - `npm run lint` exits 0
    - `npm run build` exits 0
    - The event-literal grep matches only `src/lib/analytics/events.ts` and `src/lib/analytics/events.test.ts`
    - `grep -rn "EVENT_TYPE_" src/` returns no matches
    - The tracking-call census totals exactly 5 call sites, distributed 2 / 1 / 1 / 1 across the four write-site files named above
    - `grep -c "trackServerEvent\|trackBrowserEvent" "src/app/(public)/book/page.tsx"` prints `0`
    - No file under `src/app/(admin)/` references `PageViewTracker`
    - The census table is written into `06-05-SUMMARY.md`
  </acceptance_criteria>
  <done>The full static gate passes, and exactly five tracking call sites exist in exactly the four expected files with no event-type literal outside `events.ts`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — all four events land and both charts populate</name>
  <read_first>
    - `.planning/phases/06-analytics/06-02-SUMMARY.md` — the session-storage key format and the recorded D-08 semantics, needed to interpret step 1's dedupe expectations
    - `.planning/phases/06-analytics/06-03-SUMMARY.md` — confirms both VIN-route insertion points, needed to interpret step 2's two-row expectation
    - `.planning/phases/06-analytics/06-04-SUMMARY.md` — confirms the honeypot and `'23505'` branches are eventless, needed to interpret steps 3 and 4
    - `.planning/ROADMAP.md` §"Phase 6: Analytics" — the four success criteria step 5 checks the charts against
  </read_first>
  <files>(no source files modified — verification only)</files>
  <action>Pause execution and hand control to the human operator. Present the `<how-to-verify>` steps below verbatim, in order. Do not attempt to run these steps autonomously — they require a browser session, DevTools access to un-hide the contact form's honeypot input, an authenticated admin login, and the Supabase SQL editor. Wait for the operator's explicit "approved" plus the observed per-event row counts before proceeding to Task 3. If the operator reports a failure, stop and report which step failed rather than attempting a fix here — the fix belongs in the plan that owns the affected file (06-02 for the tracker, 06-03 for the VIN route, 06-04 for the Server Actions).</action>
  <what-built>
    Phase 6 wired four analytics write sites into the running application:
    - `page_view` — fires from the browser on every public route, deduped per path per session (`PageViewTracker` mounted in `(public)/layout.tsx`)
    - `vin_search` — fires server-side from `/api/vin/[vin]` on both success branches, including the `vin_cache` hit
    - `contact_submit` — fires inside `createContact`, success path only
    - `booking_created` — fires inside `createBooking`, success path only

    All four resolve their `event_type` string from `src/lib/analytics/events.ts`, which `src/lib/dashboard/dashboard-queries.ts` now imports instead of its own private consts — closing the STATE.md silent-failure blocker.

    Nothing here can be proven by an automated test in this repo: there is no component-test infrastructure (no `@testing-library/react`, no jsdom) and no Supabase test harness. Runtime confirmation is manual by design, per CONTEXT.md.
  </what-built>
  <how-to-verify>
    Start the dev server on the canonical port: `npm run dev` (http://localhost:3000).

    Keep the Supabase dashboard's SQL editor open for project `kyhvgskeihtccylpdkas`. The reference query throughout is:
    `select event_type, count(*), max(created_at) from analytics_events group by event_type order by event_type;`

    **0. Baseline.** Run the query and record the starting counts (likely zero rows). Every step below is a delta against this.

    **1. `page_view` — fires, and dedupes (ANLY-02, D-08).**
       a. Visit http://localhost:3000/ then navigate to /about, then /contact, then /book using the on-page navigation (not a hard reload).
       b. Re-run the query. Expect `page_view` to have increased by exactly **4** — one per distinct path.
       c. Navigate back to / and then to /about again in the same tab. Re-run the query. Expect **no further increase** — the session dedupe (D-08) means a repeat path writes nothing.
       d. Open a fresh incognito window, visit http://localhost:3000/. Expect `page_view` +1 — a new session re-fires.
       e. Confirm the `page` column holds the pathnames: `select page, count(*) from analytics_events where event_type = 'page_view' group by page;` should show `/`, `/about`, `/contact`, `/book`.
       f. With DevTools open throughout, confirm the browser console shows **no** tracking-related output — no errors, no logs (D-12). Confirm no page felt slower to render.

    **2. `vin_search` — both branches, including the cache hit (ANLY-03, D-15). This is the phase's highest-risk check.**
       a. On the home page's estimate section, submit a valid 17-character VIN that has **not** been decoded before, so it misses `vin_cache`. Confirm the estimate renders as it always did.
       b. Re-run the query. Expect `vin_search` +1.
       c. Submit **the exact same VIN a second time**. This one is served from the `vin_cache` early-return without calling NHTSA.
       d. Re-run the query. Expect `vin_search` +1 again — **total +2 for the same VIN**. ⚠ If the second submission produced no row, only one of the route's two success branches was wired and the ADMIN-04 chart will *drop* as caching improves. That is a phase-blocking failure.
       e. Confirm the `vin` column is populated: `select vin from analytics_events where event_type = 'vin_search' order by created_at desc limit 2;` — both rows should hold the same normalized uppercase VIN.
       f. Submit a clearly invalid VIN (e.g. `00000000000000000`). Confirm the UI shows its usual not-found / manual-entry path, and re-run the query: expect **no** new `vin_search` row (D-14).

    **3. `contact_submit` — and the honeypot exclusion (ANLY-04, D-11).**
       a. Submit the contact form at /contact with valid details. Confirm the usual success confirmation appears.
       b. Re-run the query. Expect `contact_submit` +1. Confirm a matching row also landed in `contacts`.
       c. ⚠ **Honeypot check.** With DevTools, un-hide the honeypot input on the contact form (it is a hidden field), type any value into it, and submit. The form will report success — that is the intended bot-fooling behavior.
       d. Re-run the query. Expect **no** new `contact_submit` row, and **no** new `contacts` row. If a row appeared, a bot can inflate this metric and the analytics table now disagrees with the source table.

    **4. `booking_created` (ANLY-05, D-11).**
       a. Book an appointment at /book for an open slot. Confirm the usual confirmation screen appears.
       b. Re-run the query. Expect `booking_created` +1, and a matching row in `bookings`.
       c. Attempt to book **the same slot again**. Expect the usual "slot taken" message and **no** new `booking_created` row — a lost race created no booking.

    **5. The charts light up (ROADMAP success criterion 3).**
       a. Log in at http://localhost:3000/admin/login and open the dashboard.
       b. Confirm the **visitors chart (ADMIN-02)** now renders data instead of the Phase 5 empty-state hint.
       c. Confirm the **VIN-search chart (ADMIN-04)** now renders data instead of the empty state.
       d. Confirm the **summary cards (ADMIN-05)** show non-zero "Total Visitors" and "VIN Searches" matching roughly what you generated above.
       e. Confirm the contacts chart (ADMIN-03) still works — it reads the real `contacts` table per 05 D-18 and must not have regressed.

    **6. Non-blocking behavior (ANLY-06).** Across every step above, confirm no flow showed a new error, no submission failed, and nothing felt slower. A tracking failure must be invisible to the user.

    Report back either "approved" with the observed counts per event type, or the specific step number that failed and what you saw instead.
  </how-to-verify>
  <acceptance_criteria>
    - All four `event_type` values are present in `analytics_events` with a count of at least 1 each
    - Step 2d confirmed: the same VIN submitted twice produced two `vin_search` rows (cache-hit branch fires — D-15)
    - Step 3d confirmed: the honeypot submission produced no `contact_submit` row and no `contacts` row (D-11)
    - Step 4c confirmed: a repeat booking of a taken slot produced no `booking_created` row (D-11)
    - Step 1c confirmed: a repeat visit to an already-seen path in the same session produced no additional row (D-08)
    - Step 2f confirmed: an invalid VIN produced no `vin_search` row (D-14)
    - Step 5b and 5c confirmed: both the visitors and VIN-search charts render data rather than the Phase 5 empty state
    - Step 1f confirmed: zero tracking-related browser console output
    - The human explicitly typed "approved"
  </acceptance_criteria>
  <resume-signal>Type "approved" with the observed per-event counts, or describe the step that failed.</resume-signal>
  <verify>
    <human-check>Operator confirms all four event types present in `analytics_events`, the repeated VIN produced two `vin_search` rows (D-15), the honeypot submission produced zero rows (D-11), and both the visitors and VIN-search charts render data instead of the Phase 5 empty state.</human-check>
    <automated>MISSING — not automatable in this repo: no component-test infrastructure (no @testing-library/react, no jsdom) and no Supabase test harness. This is a pre-existing standing gap recorded in STATE.md, not one introduced by Phase 6. Manual UAT is the intended coverage per 06-CONTEXT.md.</automated>
  </verify>
  <done>The human operator has typed "approved" and reported per-event row counts showing at least one row for each of `page_view`, `vin_search`, `contact_submit` and `booking_created`, with the D-15 two-row and D-11 zero-row checks explicitly confirmed.</done>
</task>

<task type="auto">
  <name>Task 3: Record phase outcome and close the STATE.md blocker</name>
  <files>.planning/phases/06-analytics/06-05-SUMMARY.md</files>
  <read_first>
    - `.planning/STATE.md` — the §Blockers entry "Analytics `event_type` literals need Phase 06 reconciliation" that this phase closes, and the standing component-test-infrastructure entry that it does not
    - `.planning/phases/06-analytics/06-01-SUMMARY.md` through `06-04-SUMMARY.md` — the four preceding SUMMARYs whose findings this one consolidates
    - The Task 2 checkpoint result — the observed per-event counts
  </read_first>
  <action>
    Write `.planning/phases/06-analytics/06-05-SUMMARY.md` consolidating the phase outcome.

    Include: the Task 1 static gate results and write-site census table; the Task 2 observed per-event row counts with the verification date; and an explicit statement for each of the ROADMAP's four Phase 6 success criteria saying whether it is met and what evidence proves it.

    Record these facts for downstream consumers, stated plainly:
    - The STATE.md blocker "Analytics `event_type` literals need Phase 06 reconciliation" is **closed** — `dashboard-queries.ts` now imports `ANALYTICS_EVENTS` from `src/lib/analytics/events.ts`, so a producer/consumer mismatch is a compile error rather than a silently empty chart.
    - The "Total Visitors" card counts **distinct page-visits per session, not unique people** (D-08). No downstream copy or documentation may describe it as unique visitors. Note whether the card's label was left as-is (CONTEXT.md flags rewording as optional discretion).
    - `PageViewTracker` is covered by manual UAT only. This repo still has zero component-test infrastructure — no `@testing-library/react`, no jsdom, no `@vitejs/plugin-react`, and `vitest.config.mts` includes only `src/**/*.test.ts`. That is a pre-existing standing gap recorded in STATE.md, not one introduced by this phase.
    - The four-event taxonomy is closed at `page_view`, `vin_search`, `contact_submit`, `booking_created` (D-04). `src/lib/analytics/events.ts` is the seam if a fifth is ever revisited.
    - `metadata` is `null` on every row this phase writes (D-02); rich payloads are deferred.
    - Failed VIN searches (`not-found`, `unreachable`) are deliberately untracked (D-14) and deferred.

    Do NOT edit `.planning/STATE.md` or `.planning/REQUIREMENTS.md` from this plan — phase-completion bookkeeping is the orchestrator's job. Just record the facts here so it has them.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-analytics/06-05-SUMMARY.md && grep -q "page_view" .planning/phases/06-analytics/06-05-SUMMARY.md && grep -q "distinct page-visits per session" .planning/phases/06-analytics/06-05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/06-analytics/06-05-SUMMARY.md` exists
    - It contains the write-site census table from Task 1
    - It contains the observed per-event row counts from Task 2, with a date
    - It addresses all four ROADMAP Phase 6 success criteria by name, each with a met/not-met verdict and its evidence
    - It contains the exact phrase `distinct page-visits per session` (D-08 semantics recorded for downstream)
    - It states that the STATE.md event-type reconciliation blocker is closed
    - It states that `PageViewTracker` is manual-UAT-only and that the component-test gap is pre-existing
    - `.planning/STATE.md` is unmodified by this plan: `git diff --name-only .planning/STATE.md` returns nothing
  </acceptance_criteria>
  <done>A dated SUMMARY records the static gate, the census, the observed runtime evidence per event type, all four success-criteria verdicts, and the D-08 semantics note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification actions → live Supabase project | This plan writes real rows into the live `kyhvgskeihtccylpdkas` project's `analytics_events`, `contacts` and `bookings` tables. |
| human operator → admin dashboard | Verification requires an authenticated admin session at `/admin/login`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | Verification polluting production data | accept | The verification deliberately creates real `contacts` and `bookings` rows — that is the only way to prove the events fire on the genuine success path. This project has a single Supabase environment and Phases 4 and 5 verified the same way. Rows are few, identifiable by their verification timestamp, and the booking occupies one real appointment slot. Mitigation is operational: the SUMMARY records what was created so the owner can remove the test booking. Revisit if a staging project is ever added. |
| T-06-05-02 | Information disclosure | Credentials or keys leaking into the SUMMARY | mitigate | The SUMMARY records row *counts*, exit codes and chart-populated verdicts only. Task 3's action forbids nothing about keys explicitly, so state it here: never paste `.env.local` values, the Supabase service-role key, the anon key, or the admin password into the SUMMARY or into any command output captured there. Phase 5 established the convention of verifying env-var presence by name only, never by value. |
| T-06-05-03 | Repudiation | Declaring the phase passed on static checks alone | mitigate | The phase's headline risk — a producer/consumer string mismatch — passes every static check and fails only at runtime with a silently empty chart. Task 2 is therefore a `gate="blocking"` human checkpoint, not an automated assertion, and its acceptance criteria name the specific observations (two rows for one repeated VIN; zero rows from the honeypot) that distinguish a real pass from a plausible-looking one. |
| T-06-05-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages. |
</threat_model>

<verification>
1. Static gate: `npx tsc --noEmit`, `npx vitest run`, `npm run lint`, `npm run build` all exit 0.
2. Write-site census returns exactly five tracking call sites across exactly four files, with no event-type literal outside `events.ts`.
3. Human checkpoint approved, with all four event types observed in `analytics_events`.
4. The D-15 two-branch check specifically confirmed: one VIN submitted twice yields two rows.
5. The D-11 honeypot check specifically confirmed: a honeypot submission yields zero rows.
6. Both previously-empty charts render data.
</verification>

<success_criteria>
All four ROADMAP Phase 6 success criteria are demonstrably met:
1. Page views on all public pages are recorded as `page_view` events without slowing page load.
2. Each successful VIN decode records a `vin_search`; each contact submission a `contact_submit`; each booking a `booking_created`.
3. The admin dashboard charts display data from `analytics_events` (visitors over time, VIN searches over time) alongside the contacts chart's real-table source.
4. Tracking is fire-and-forget: no failure blocked or errored the user action that triggered it.

Plus: the STATE.md event-type reconciliation blocker is closed, and the D-08 "distinct page-visits per session" semantics are recorded for downstream.
</success_criteria>

<output>
Create `.planning/phases/06-analytics/06-05-SUMMARY.md` when done.
</output>
