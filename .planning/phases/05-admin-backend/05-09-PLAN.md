---
phase: 05-admin-backend
plan: 09
type: execute
wave: 6
depends_on: [05-04, 05-05, 05-07, 05-08]
files_modified: []
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04, ADMIN-05, ADMIN-06, ADMIN-07, USER-01, USER-02, USER-03, USER-04]
user_setup: []

must_haves:
  truths:
    - "All five ROADMAP success criteria for Phase 5 are confirmed by a human against a running app with .env.local loaded"
    - "Both D-10 guard refusals are exercised against the live app, not just unit-tested"
    - "No admin lockout resulted from the verification run"
    - "Verifies D-06: no migration was added this phase. D-10: both guard refusals are exercised live. D-14: /admin/login renders outside the sidebar shell. D-15: the dashboard tables carry no row actions. D-09: login lands on /admin."
  artifacts: []
  key_links: []
---

<objective>
Verify all five of Phase 5's ROADMAP success criteria against the running application, exercise both D-10 guard refusals live, and confirm no admin lockout resulted.

Purpose: this repo has zero component-test infrastructure — all tests are pure-function `vitest`, so every UI behavior in this phase is protected by manual UAT only. Two things in particular cannot be asserted by any automated check available here: that the login page renders without authenticated chrome (already checked in `05-05` and re-confirmed end-to-end here), and that both D-10 guards actually refuse at the live surface rather than merely returning `true` in a unit test. The guards matter more than usual: D-08 leaves no password-reset flow, so an admin lockout is recoverable only from the Supabase dashboard.

Output: recorded pass/fail per success criterion, per D-10 guard, and a confirmed post-run admin count. No files change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-admin-backend/05-CONTEXT.md
@.planning/phases/05-admin-backend/05-04-SUMMARY.md
@.planning/phases/05-admin-backend/05-05-SUMMARY.md
@.planning/phases/05-admin-backend/05-07-SUMMARY.md
@.planning/phases/05-admin-backend/05-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run every automated gate and pre-stage the manual checks</name>
  <read_first>
    - `.planning/ROADMAP.md` — the `### Phase 5: Admin Backend` block, specifically the five numbered Success Criteria this plan verifies verbatim
    - `.planning/phases/05-admin-backend/05-04-SUMMARY.md` — the recorded env-guard caveat: `src/lib/supabase/middleware.ts` lines 8-12 skip auth entirely when Supabase env vars are absent, so criterion 1 must be checked with `.env.local` loaded or a working guard will look broken
    - `.planning/phases/05-admin-backend/05-07-SUMMARY.md` — the `data-testid` list for every card, chart and table
    - `.planning/phases/05-admin-backend/05-08-SUMMARY.md` — the `data-testid` list for the user-management surface and the confirmation that both guards run before `deleteUser()`
    - `.planning/phases/05-admin-backend/05-01-SUMMARY.md` — the `auth.users` count established at the start of the phase
  </read_first>
  <action>
    Run every automated gate and report each result before any human interaction:
    - `npm run lint` — must exit 0
    - `npx tsc --noEmit` — must exit 0
    - `npm run build` — must exit 0
    - `npx vitest run` — must exit 0, and report the total test count so the four suites added in plan `05-03` are visibly present alongside the prior 33

    Then re-assert the three phase-wide invariants by grep, since they span files owned by different plans and no single plan's own criteria cover the whole tree:
    - `grep -rn "getSession" src/` returns nothing (AUTH-03's `getUser()` choice holds everywhere, not just in middleware)
    - `grep -rl "SUPABASE_SERVICE_ROLE_KEY" src/` lists only `src/lib/supabase/admin.ts` (USER-04)
    - `grep -rn "redirectTo" src/lib/auth/ src/components/auth/` returns nothing (D-09's open-redirect avoidance)
    - `test ! -f "src/app/(admin)/admin/layout.tsx"` succeeds (D-14's route-group split still intact)
    - `git status --porcelain supabase/` shows no additions (D-06: no migration added this phase)

    Then start the dev server on port 3000 with `.env.local` present and, while logged out, confirm by `curl` that `/admin` and `/admin/users` both return a 307 or 308 redirecting to `/admin/login`, and that `/admin/login` returns 200 without redirecting.

    Report all results. If any automated gate fails, stop and report rather than proceeding to the human checkpoint — asking a person to walk five criteria against a build that does not compile wastes their time.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npm run build && npx vitest run && ! grep -rn "getSession" src/ && test ! -f "src/app/(admin)/admin/layout.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint`, `npx tsc --noEmit`, `npm run build` and `npx vitest run` all exit 0, and each result is reported
    - The reported `vitest` total test count exceeds 33
    - `grep -rn "getSession" src/` returns no matches
    - `grep -rl "SUPABASE_SERVICE_ROLE_KEY" src/` lists only `src/lib/supabase/admin.ts`
    - `grep -rn "redirectTo" src/lib/auth/ src/components/auth/` returns no matches
    - `src/app/(admin)/admin/layout.tsx` does not exist
    - `git status --porcelain supabase/` shows no additions
    - `curl` against `/admin` and `/admin/users` while logged out each return 307 or 308 targeting `/admin/login`; `/admin/login` returns 200
  </acceptance_criteria>
  <done>All four automated gates exit 0, all five phase-wide invariants hold, the logged-out redirect fires for both protected routes with the login page exempt, and every result is reported.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the five ROADMAP success criteria and both D-10 guards</name>
  <read_first>
    - `.planning/ROADMAP.md` — the five Success Criteria, to be walked in order and reported individually
    - `.planning/phases/05-admin-backend/05-01-SUMMARY.md` — the credentials situation and the starting `auth.users` count
    - `.planning/phases/05-admin-backend/05-08-SUMMARY.md` — the user-management `data-testid` list and the guard-ordering confirmation
  </read_first>
  <action>
    Everything automatable has already run in Task 1. Present the five criteria as a numbered walkthrough and pause for the results. Do not mark any criterion passed on the strength of code inspection — this checkpoint exists because no component-test infrastructure covers these behaviors.

    Two things to state clearly before pausing:
    - The dev server must be running with `.env.local` loaded. Without it the middleware guard is disabled by design and criterion 1 will appear to fail when it is simply switched off.
    - Step 5 involves a real, irreversible account deletion. It must be performed on a throwaway account created during step 5 itself, never on the original admin from plan `05-01`. There is no password-reset flow (D-08), so a lost original account is recoverable only from the Supabase dashboard.

    After the human reports, record each criterion as pass or fail with their exact wording, record both guard-refusal messages verbatim, and record the final `auth.users` count. If any criterion fails, do not mark the phase complete — capture the specific failure so a gap-closure plan can target it.
  </action>
  <what-built>
    The full Phase 5 surface is in place:
    - `/admin/login` — real email+password form wired to `loginAction`, always redirecting to `/admin` on success (D-09, no `redirectTo`)
    - The `(dashboard)` sidebar shell (Dashboard, Users, signed-in email, logout) wrapping `/admin` and `/admin/users` but not `/admin/login` (D-14)
    - `/admin` — four summary cards, three charts, two read-only tables
    - `/admin/users` — the admin list, add-admin form, and per-row removal behind a confirmation dialog

    Expected-but-correct behavior worth knowing before you look:
    - The **visitors** and **VIN searches** cards show `0` with a "tracking starts in Phase 6" hint, and their two charts show an explicit "no data yet" message. This is D-01 and D-02 working as designed — event tracking is Phase 6's job (ANLY-02..06). A fabricated number there would be the bug.
    - The **contacts** and **bookings** cards show real counts, and the **contacts chart** shows real data, because those tables hold real Phase 4 rows. That asymmetry is D-18, resolved deliberately: a contacts card showing a real count above a contacts chart reading "no data" would look like a bug.
    - Neither dashboard table has row actions. That is D-15; booking status transitions were deliberately deferred.
  </what-built>
  <how-to-verify>
    **Criterion 1 — logged-out `/admin/*` redirects to `/admin/login` with no flash of admin content.**
    1. In a logged-out browser (private window), visit http://localhost:3000/admin. Confirm you land on `/admin/login`.
    2. Watch carefully for a flash of the sidebar or dashboard content before the redirect. There should be none — the middleware redirects before any Server Component renders.
    3. Repeat with http://localhost:3000/admin/users.

    **Criterion 2 — valid login lands on the dashboard; logout clears the session and returns to login.**
    4. Log in with the credentials from plan `05-01`. Confirm you land on `/admin` with the sidebar showing your email.
    5. First try a deliberately wrong password. Confirm the error message does not indicate whether the email exists, and that the same message appears for a completely made-up email address. These must be indistinguishable.
    6. Click logout. Confirm you return to `/admin/login`.
    7. Immediately press the browser Back button. Confirm you do NOT see cached dashboard content — you should be redirected to login again.

    **Criterion 3 — summary cards and charts for each metric over time.**
    8. Log back in. Confirm four cards: visitors, contacts, VIN searches, bookings.
    9. Confirm the visitors and VIN-search cards show `0` with the Phase 6 tracking hint, and that the contacts and bookings cards do NOT show that hint.
    10. Confirm three charts are present. The visitors and VIN-search charts should show an explicit "no data yet" message — not a blank box, not a broken or empty axis. The contacts chart should show real data.

    **Criterion 4 — a table of recent contact submissions and a table of upcoming appointments.**
    11. Confirm both tables render. If your database has contacts or future bookings, confirm rows appear; if not, confirm each shows a clear "nothing yet" message rather than an empty frame.
    12. On the appointments table, confirm each row shows date, time, name, phone, vehicle description and status — enough to make a confirmation call without opening Supabase.
    13. Confirm neither table offers any clickable row action (no edit, no delete, no status change). Their absence is intentional.

    **Criterion 5 — an admin can add a new admin account and remove an existing one.**
    14. Go to `/admin/users`. Confirm your own account is listed with its email, created date and last sign-in.
    15. Add a throwaway admin (for example `throwaway@example.com`) with a password of at least 8 characters. First deliberately mismatch the confirm field and confirm the error appears on the confirm-password field. Then submit correctly.
    16. Confirm the password is displayed once on success with a notice that it will not be shown again, and confirm the new account appears in the list showing "Never signed in" rather than "Invalid Date".
    17. **Guard 1 — self-delete.** Attempt to remove YOUR OWN account. If the control is hidden for your row, say so. If it is available, click it and confirm the refusal message. Report the exact wording. Your account must still exist afterward.
    18. **Guard 2 — last admin.** Remove the throwaway account first (confirm the dialog names `throwaway@example.com` specifically, not "this user"), leaving one admin. Then attempt to remove your own account again and confirm you are refused. Report the exact wording.
    19. Confirm your original account still works: log out and log back in.
    20. Report the final admin count visible at `/admin/users`.
  </how-to-verify>
  <acceptance_criteria>
    - Each of the five criteria is recorded individually as pass or fail with the human's exact wording
    - Criterion 1 is confirmed for both `/admin` and `/admin/users`, including the no-flash observation
    - Criterion 2 records that a wrong password and a non-existent email produce the same message, and that Back after logout does not show cached dashboard content
    - Criterion 3 records that the Phase 6 hint appears on exactly the visitors and VIN-search cards, and that the two analytics charts show an explicit message rather than a blank box
    - Criterion 4 records all six D-17 appointment columns and the absence of row actions
    - Criterion 5 records: the confirm-password field error, the show-once password display, the "Never signed in" value, the removal dialog naming the throwaway email specifically, and both guard-refusal messages verbatim
    - The final `auth.users` count is recorded and is >= 1
    - The original admin account from plan `05-01` still exists and can log in
    - No repository file changed — `git status --porcelain` output is unchanged from before the plan started
    - Any failure is recorded with enough specificity to target a gap-closure plan
  </acceptance_criteria>
  <done>All five ROADMAP success criteria are recorded as passed by the human, both D-10 guard refusals were exercised live with their messages captured verbatim, the original admin account still works, and no repository file changed.</done>
  <resume-signal>Report pass/fail for each of the 5 criteria plus both guard messages and the final admin count. Do not include any password.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification run → live Supabase Auth | This plan performs a real, irreversible account deletion against the production auth store |
| Human report → phase summary | Reported evidence must not carry credential material into a version-controlled artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Denial of Service | Verification itself locking every admin out | mitigate | Step 5 deletes only a throwaway account created during that step; step 19 re-verifies the original account can still log in, and the final count is asserted `>= 1`. D-08 leaves no reset flow, so this is the run's highest-consequence risk |
| T-05-09-02 | Information Disclosure | A password reaching the version-controlled summary | mitigate | The resume signal explicitly forbids including any password; acceptance criteria record guard messages and counts only. The throwaway account's password has no lasting value after step 18 removes it |
| T-05-09-03 | Information Disclosure | Account enumeration surviving to production | mitigate | Step 5 directly probes it: a wrong password and a wholly fictitious email must produce the identical message. This is the only place the non-enumeration property is verified against the rendered UI rather than by reading code |
| T-05-09-04 | Session Management | Cached authenticated RSC payload served after logout | mitigate | Step 7 exercises the browser Back button after logout, verifying `logoutAction`'s `revalidatePath('/', 'layout')` actually invalidated the payload rather than merely being present in source |
| T-05-09-05 | Elevation of Privilege | The middleware guard verified against a disabled state | mitigate | Task 1 requires `.env.local` loaded, per the caveat recorded in `05-04-SUMMARY.md`, so criterion 1 is checked against an active guard rather than the env-guard escape hatch |
| T-05-09-06 | Tampering | Guard enforcement verified only in the UI layer | mitigate | Step 17 asks the human to report whether the control was hidden versus refused. A hidden-only control is recorded as such, since D-10 requires server-side enforcement and plan `05-08`'s grep criteria already assert both predicates run before `deleteUser()` |
</threat_model>

<verification>
`npm run lint`, `npx tsc --noEmit`, `npm run build` and `npx vitest run` all exit 0. All five phase-wide grep/file invariants hold. A human has recorded pass/fail for each of the five ROADMAP success criteria and captured both D-10 guard-refusal messages verbatim. The final `auth.users` count is >= 1 and the original admin can still log in.
</verification>

<success_criteria>
Every Phase 5 ROADMAP success criterion is human-confirmed against a running app with the middleware guard active; both D-10 guards are shown to refuse at the live surface; no admin lockout occurred; and no repository file changed during verification.
</success_criteria>

<output>
Create `.planning/phases/05-admin-backend/05-09-SUMMARY.md` when done. Record pass/fail per criterion, both guard messages verbatim, and the final admin count. Any failure recorded here is the input to a `/gsd:plan-phase 05 --gaps` run.
</output>
