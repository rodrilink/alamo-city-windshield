---
phase: 05-admin-backend
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/ui/chart.tsx
  - src/components/ui/alert-dialog.tsx
  - src/components/ui/table.tsx
  - package.json
  - package-lock.json
autonomous: true
requirements: [ADMIN-02, ADMIN-03, ADMIN-04, ADMIN-06, ADMIN-07, USER-03]
user_setup: []

must_haves:
  truths:
    - "The three shadcn primitives exist in src/components/ui/ and the project still builds"
    - "No generated primitive imports from @radix-ui — this repo is Base UI, not Radix"
  artifacts:
    - path: "src/components/ui/chart.tsx"
      provides: "ChartContainer / ChartTooltip / ChartLegend wrappers over Recharts"
      exports: ["ChartContainer", "ChartTooltip", "ChartTooltipContent", "ChartConfig"]
    - path: "src/components/ui/alert-dialog.tsx"
      provides: "Destructive-confirmation primitive for D-11"
      exports: ["AlertDialog", "AlertDialogTrigger", "AlertDialogContent", "AlertDialogAction", "AlertDialogCancel"]
    - path: "src/components/ui/table.tsx"
      provides: "Semantic table wrappers for ADMIN-06 / ADMIN-07"
      exports: ["Table", "TableHeader", "TableBody", "TableRow", "TableHead", "TableCell"]
  key_links:
    - from: "package.json"
      to: "recharts"
      via: "dependencies entry added by the shadcn chart add"
      pattern: "\"recharts\""
    - from: "src/components/ui/alert-dialog.tsx"
      to: "@base-ui/react/alert-dialog"
      via: "primitive import"
      pattern: "@base-ui/react"
---

<objective>
Generate the three shadcn UI primitives this phase needs — `chart`, `alert-dialog`, `table` — and verify each one landed on this repo's Base UI convention rather than the Radix convention that generic shadcn documentation shows.

Purpose: `05-PATTERNS.md` classifies `chart.tsx` as the single largest net-new surface in the phase (no chart, no `recharts` import, and no data-visualization component exists anywhere in the repo). `alert-dialog.tsx` and `table.tsx` are likewise absent. Every downstream plan that renders a chart (`05-07`) or a destructive-confirmation dialog (`05-08`) or a dashboard table (`05-07`) imports from these files, so they must exist and be verified first. This repo's `components.json` sets `"style": "base-nova"`, meaning every primitive wraps `@base-ui/react` and uses a `render` prop where Radix uses `asChild` — Phase 4 already hit this mismatch once with `calendar.tsx` and `form.tsx`.

Output: three new files under `src/components/ui/`, `recharts` added to `package.json`, and a clean `npm run lint` / `npx tsc --noEmit`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-admin-backend/05-RESEARCH.md
@.planning/phases/05-admin-backend/05-PATTERNS.md
@.planning/phases/05-admin-backend/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate chart, alert-dialog and table primitives via the shadcn CLI</name>
  <files>src/components/ui/chart.tsx, src/components/ui/alert-dialog.tsx, src/components/ui/table.tsx, package.json</files>
  <read_first>
    - `.planning/phases/05-admin-backend/05-01-SUMMARY.md` — confirm the human approved `recharts`. If it was rejected, do not run the `chart` add; generate only `alert-dialog` and `table` and record the reduced scope.
    - `F:/2026/Projects/Windshield/components.json` — confirm `"style": "base-nova"` and the configured alias/path targets so the CLI writes into `src/components/ui/`
    - `F:/2026/Projects/Windshield/src/components/ui/sheet.tsx` — the in-repo reference for the Base UI convention: it imports `Dialog as SheetPrimitive` from `@base-ui/react/dialog` and passes `render={<Button variant="ghost" size="icon-sm" />}` on `SheetPrimitive.Close` instead of Radix's `asChild`
    - `F:/2026/Projects/Windshield/src/components/ui/card.tsx` — the in-repo reference for the `data-slot` naming convention (`data-slot="card-header"`, `"card-title"`, one per exported sub-component)
    - `F:/2026/Projects/Windshield/package.json` — the pre-install dependency baseline, so the diff after the add is attributable
  </read_first>
  <action>
    Run `npx shadcn@latest add chart alert-dialog table` from the repo root. Generate `alert-dialog`, not `dialog`: `05-RESEARCH.md`'s Alternatives Considered table recommends it for D-11 because it ships `AlertDialogAction` and `AlertDialogCancel` with Button-variant wiring already composed for a destructive confirmation, which `dialog.tsx` would require hand-assembling.

    Expect the CLI to write `src/components/ui/chart.tsx`, `src/components/ui/alert-dialog.tsx`, `src/components/ui/table.tsx`, and to add `recharts` to `package.json` dependencies as a side effect of the `chart` add. Let the CLI resolve the `recharts` version — do not hand-pin it. The registry declares `recharts@3.8.0` as a floor; `npm view recharts version` returned `3.10.1` on 2026-08-06.

    Both `chart.json` and `alert-dialog.json` declare `registryDependencies` that already exist locally (`card` and `button` respectively) — if the CLI offers to overwrite `src/components/ui/card.tsx` or `src/components/ui/button.tsx`, decline. Those files are in use by Phase 2/3/4 code and an overwrite would silently revert local adaptations. `card.tsx` in particular is the ADMIN-05 summary-card primitive and must stay as-is.

    After the add completes, report the exact `recharts` version string that landed in `package.json`, and list any file the CLI touched beyond the three expected new files.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies.recharts) { throw new Error('recharts missing from dependencies'); } console.log('recharts', p.dependencies.recharts)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/chart.tsx` exists and contains `export` declarations for `ChartContainer` and `ChartTooltipContent`, plus a `ChartConfig` type export
    - `src/components/ui/alert-dialog.tsx` exists and contains `AlertDialogAction` and `AlertDialogCancel` exports
    - `src/components/ui/table.tsx` exists and contains `Table`, `TableHeader`, `TableBody`, `TableRow`, `TableHead`, `TableCell` exports
    - `package.json` `dependencies` contains a `"recharts"` key
    - `git diff --name-only` lists no change to `src/components/ui/card.tsx` or `src/components/ui/button.tsx`
    - The summary records the exact resolved `recharts` version string
  </acceptance_criteria>
  <done>The three primitive files exist with the expected exports, `recharts` is in `package.json` dependencies, and neither `card.tsx` nor `button.tsx` was overwritten.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Base UI convention and clean compile</name>
  <files>src/components/ui/chart.tsx, src/components/ui/alert-dialog.tsx, src/components/ui/table.tsx</files>
  <read_first>
    - `F:/2026/Projects/Windshield/src/components/ui/chart.tsx` — read the file the CLI just wrote, in full
    - `F:/2026/Projects/Windshield/src/components/ui/alert-dialog.tsx` — read the file the CLI just wrote, in full
    - `F:/2026/Projects/Windshield/src/components/ui/table.tsx` — read the file the CLI just wrote, in full
    - `F:/2026/Projects/Windshield/src/components/ui/sheet.tsx` — the Base UI `render`-prop and `data-slot` reference to compare against
    - `.planning/phases/05-admin-backend/05-RESEARCH.md` — Pattern 1, which documents the expected `render={<Button variant={variant} size={size} />}` shape on `AlertDialogCancel` as confirmed against the live registry payload
  </read_first>
  <action>
    Verify the three generated files match this repo's conventions rather than the Radix-oriented shape shown in generic shadcn documentation. This is the same post-generation verification Phase 4 applied to `calendar.tsx`.

    Check four things concretely:

    1. **No Radix imports.** This repo has zero `@radix-ui/*` dependencies, so any such import would be an unresolvable module and a build failure. Grep all three files for `@radix-ui`. Expect zero matches. If a match exists, rewrite the import to the corresponding `@base-ui/react/<primitive>` path and convert any `asChild` usage to the `render` prop, following `sheet.tsx`'s `SheetPrimitive.Close` example.

    2. **`alert-dialog.tsx` imports from `@base-ui/react/alert-dialog`** and uses the `render` prop (not `asChild`) on `AlertDialogCancel`/`AlertDialogAction` where those elements render as a `Button`.

    3. **`data-slot` attributes follow `<primitive-name>-<part>`.** `table.tsx` should carry `data-slot="table"`, `"table-header"`, `"table-body"`, `"table-row"`, `"table-head"`, `"table-cell"`; `alert-dialog.tsx` should carry `data-slot="alert-dialog"`, `"alert-dialog-content"`, `"alert-dialog-cancel"` and siblings. This matches `card.tsx` and `sheet.tsx`. Do not invent slot names the CLI did not emit; only report a mismatch if a sub-component is missing its attribute entirely.

    4. **`chart.tsx` imports `recharts`** and does not import any charting library other than `recharts`.

    Then confirm the project still compiles and lints: run `npx tsc --noEmit` and `npm run lint`. Both must exit 0. If `chart.tsx` produces a type error against the installed `recharts` major (the A1 risk logged in `05-RESEARCH.md`'s Assumptions Log), fix the generated file rather than downgrading `recharts`, and document the fix in the summary the way `form.tsx`'s header comment documents its own hand-adaptation.

    Do not add a consumer component in this plan — no chart, dialog or table is rendered yet. This plan proves the primitives compile in isolation.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -rl "@radix-ui" src/components/ui/chart.tsx src/components/ui/alert-dialog.tsx src/components/ui/table.tsx; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@radix-ui" src/components/ui/chart.tsx src/components/ui/alert-dialog.tsx src/components/ui/table.tsx` reports 0 matches in all three files
    - `src/components/ui/alert-dialog.tsx` contains the string `@base-ui/react/alert-dialog`
    - `src/components/ui/alert-dialog.tsx` contains `render=` on the cancel sub-component and contains no occurrence of `asChild`
    - `src/components/ui/chart.tsx` contains the string `recharts`
    - `src/components/ui/table.tsx` contains `data-slot="table-row"` and `data-slot="table-cell"`
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
    - Any hand-edit applied to a generated file is documented in a header comment in that file naming what was changed and why
  </acceptance_criteria>
  <done>All three primitives are free of `@radix-ui` imports, `alert-dialog.tsx` uses the Base UI `render` prop, `npx tsc --noEmit` and `npm run lint` both exit 0, and any hand-edit is documented in-file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn registry (`ui.shadcn.com/r/styles/base-nova/*.json`) → `src/components/ui/*.tsx` | Remotely fetched source is written into the repo and compiled into the production bundle |
| npm registry → `node_modules` → browser bundle | `recharts` ships client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | `npm install recharts` via `npx shadcn@latest add chart` | mitigate | Cleared by the blocking human checkpoint in plan `05-01` (slopcheck `[OK]`, repo URL and spelling verified on npmjs.com). Task 1 refuses to run the `chart` add if `05-01-SUMMARY.md` records a rejection |
| T-05-02-01 | Tampering | Generated primitive source in `src/components/ui/` | mitigate | Task 2 reads all three generated files in full, greps for `@radix-ui` imports, and confirms `chart.tsx` imports no charting library other than `recharts` — an unexpected import is a visible signal, not a silent addition |
| T-05-02-02 | Tampering | CLI overwrite of in-use `card.tsx` / `button.tsx` | mitigate | Task 1 declines overwrite prompts for both files and Task 1's acceptance criteria assert `git diff --name-only` lists neither, so a silent revert of Phase 2/3/4 adaptations is caught |
| T-05-02-03 | Denial of Service | `recharts` major-version drift breaking the generated `chart.tsx` (Assumptions Log A1) | accept | Both the registry floor (`3.8.0`) and npm latest (`3.10.1`) are within the same major; the `ChartConfig`/`ChartContainer` API is stable across 3.x. A breakage surfaces immediately as a `tsc`/lint failure in Task 2, not as a silent runtime bug |
</threat_model>

<verification>
`npx tsc --noEmit` exits 0. `npm run lint` exits 0. No `@radix-ui` import exists in any of the three generated files. `recharts` is present in `package.json` dependencies. `card.tsx` and `button.tsx` are unmodified.
</verification>

<success_criteria>
`src/components/ui/chart.tsx`, `src/components/ui/alert-dialog.tsx` and `src/components/ui/table.tsx` all exist, export the components downstream plans import, use `@base-ui/react` (not Radix), and the project type-checks and lints clean.
</success_criteria>

<output>
Create `.planning/phases/05-admin-backend/05-02-SUMMARY.md` when done. Record the resolved `recharts` version, the exact export list of each generated file (plans `05-07` and `05-08` import from them), and any hand-edit applied.
</output>
