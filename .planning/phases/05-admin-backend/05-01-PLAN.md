---
phase: 05-admin-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [ADMIN-02, ADMIN-03, ADMIN-04, AUTH-01]
user_setup:
  - service: supabase
    why: "AUTH-01 (login) cannot be verified unless at least one auth.users row exists. Phase 1 D-12 assigned first-admin creation to the owner via the Supabase dashboard; no repo artifact documents that step ever running."
    dashboard_config:
      - task: "Confirm at least one user exists, or create one (email + password)"
        location: "Supabase Dashboard -> Authentication -> Users"

must_haves:
  truths:
    - "A human has explicitly approved installing recharts before any install command runs"
    - "A human has confirmed at least one auth.users row exists in the live Supabase project"
  artifacts: []
  key_links: []
---

<objective>
Clear the two human gates this phase cannot proceed past on its own: approval of the single new npm package (`recharts`), and confirmation that at least one Supabase Auth user exists to log in as.

Purpose: `05-RESEARCH.md`'s Package Legitimacy Audit found exactly one new npm dependency for this phase — `recharts`, pulled in as a side effect of `npx shadcn@latest add chart`. This project has an established blocking package-approval gate (`03-01-PLAN.md`, `04-01-PLAN.md`) that must run before any install. Separately, D-19 requires a human action: no plan, summary, or state file in this repository documents Phase 1 D-12's "create the first admin manually in the Supabase dashboard" step ever running. D-05 makes `auth.users` the entire admin list, so with zero rows there is nothing to log in as and AUTH-01 is unverifiable regardless of how correct the login code is.

Output: Recorded human approval (or rejection) of the `recharts` install, and recorded confirmation of the `auth.users` row count. No repository files change in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-admin-backend/05-RESEARCH.md
@.planning/phases/05-admin-backend/05-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve the recharts npm install</name>
  <read_first>
    - `.planning/phases/05-admin-backend/05-RESEARCH.md` — the `## Package Legitimacy Audit` section (recharts verdict `[OK]`, registry pin `3.8.0`, npm latest `3.10.1`, the slopcheck Windows-subprocess note) and the `## Standard Stack` table
    - `.planning/phases/05-admin-backend/05-CONTEXT.md` — the `## ⚠ New dependencies required — needs owner approval` section
    - `F:/2026/Projects/Windshield/package.json` — confirm directly that `recharts` appears in neither `dependencies` nor `devDependencies`
    - `.planning/phases/04-booking-contact/04-01-PLAN.md` — the shape of the prior gate to mirror (evidence block, npmjs.com verification steps, verbatim-decision recording)
  </read_first>
  <action>
    Do not install anything and do not run `npx shadcn@latest add` in this plan. Present `recharts` to the developer with the evidence in `<what-built>`, wait for an explicit decision, and record that decision verbatim in the plan summary.

    Before presenting, re-verify absence by reading `package.json` directly. The currently installed dependency set is: `@base-ui/react@1.3.0`, `@hookform/resolvers@^5.7.1`, `@supabase/ssr@0.10.2`, `@supabase/supabase-js@2.103.0`, `class-variance-authority@0.7.1`, `clsx@2.1.1`, `date-fns@^4.4.0`, `lucide-react@1.8.0`, `motion@12.38.0`, `next@15.5.15`, `react@19.1.0`, `react-day-picker@^10.0.1`, `react-dom@19.1.0`, `react-hook-form@^7.84.0`, `server-only@^0.0.1`, `shadcn@4.2.0`, `tailwind-merge@3.5.0`, `tw-animate-css@1.4.0`, `zod@^4.4.3`; dev deps include `vitest@^4.1.10`. If `recharts` IS in fact already present, say so explicitly rather than asking for approval to install it again.

    State plainly that this gate covers ONE npm package, not five like `04-01`. The other three additions this phase makes (`src/components/ui/chart.tsx`, `src/components/ui/alert-dialog.tsx`, `src/components/ui/table.tsx`) are shadcn CLI-generated source files written into this repo, not npm dependencies: per the live registry JSON, `table.json` declares no `dependencies` array at all, and `alert-dialog.json` depends only on `@base-ui/react`, which is already installed at `1.3.0`.

    Per the package legitimacy protocol, a legitimacy checkpoint is never auto-approvable — `workflow.auto_advance` does not apply and this checkpoint must block on a real human response.
  </action>
  <what-built>
    Nothing has been installed yet. This is a pre-install gate.

    **`recharts`** — production dependency, one package.
    - Purpose: the SVG chart engine underneath shadcn's `ChartContainer` / `ChartTooltip` / `ChartLegend`. ADMIN-02 (visitors over time), ADMIN-03 (contact submissions over time) and ADMIN-04 (VIN searches over time) all require charts, and `REQUIREMENTS.md` names "shadcn Chart / Recharts" in ADMIN-02 itself.
    - Version: shadcn's `chart.json` registry payload declares `"dependencies": ["recharts@3.8.0"]` — a floor, not a pin. `npm view recharts version` returned `3.10.1` on 2026-08-06. The shadcn CLI resolves the actual installed version; per `05-CONTEXT.md`'s own note, let the CLI choose it rather than hand-pinning, to avoid a v2/v3 mismatch against the generated `chart.tsx`.
    - Audit status: **audited in `05-RESEARCH.md`, verdict `[OK]` / Approved.** `slopcheck install recharts` emitted `[OK]` (1 package scanned, 1 OK) before its own internal `npm install` sub-step failed with a Windows subprocess spawn error (`FileNotFoundError: WinError 2`) — that failure is slopcheck's install attempt, not a rejection of the package. The legitimacy verdict was already emitted.
    - Provenance: source repo `github.com/recharts/recharts`, confirmed against the live npm registry. `recharts` is also named in this project's own `CLAUDE.md` recommended stack, and confirmed via the official shadcn registry JSON — an authoritative source, not training-data recall.
    - Nothing else: the form stack (`react-hook-form`, `zod`, `@hookform/resolvers`) is already installed from Phase 4, `date-fns@^4.4.0` is already installed and covers the 30-day bucket arithmetic, and D-04 uses built-in Server Components plus Server Actions rather than any data-fetching library.

    **What this gate does NOT cover (no npm package involved):** plan `05-02` will run `npx shadcn@latest add chart alert-dialog table`, which writes three source files into `src/components/ui/`. Only the `chart` add pulls a runtime dependency (`recharts`, the subject of this gate). `alert-dialog` needs only the already-installed `@base-ui/react`; `table` needs nothing at all.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/recharts and confirm the repository link points to `github.com/recharts/recharts`.
    2. Confirm the listed weekly download volume is in the millions and the package has a multi-year release history — this is the long-established React charting library, not a recent lookalike.
    3. Confirm the latest published version is in the `3.x` line.
    4. Confirm the spelling in the install path is exactly `recharts` — not `re-charts`, `recharts-js`, `react-charts`, or `@recharts/recharts`.
    5. Confirm you accept that `05-02` will run `npx shadcn@latest add chart alert-dialog table`, and that the `chart` add will install `recharts` as a side effect (the shadcn CLI resolves the version).
  </how-to-verify>
  <acceptance_criteria>
    - Human has responded with an explicit approval or rejection decision for `recharts`
    - The summary records that decision verbatim
    - If rejected: the summary states that ADMIN-02, ADMIN-03 and ADMIN-04 are consequently blocked, and that plan `05-02` must not run `npx shadcn@latest add chart`
    - `git status --porcelain` output is unchanged from before the plan started (no file modified by this task)
  </acceptance_criteria>
  <done>The developer has explicitly approved or rejected `recharts`, the decision is recorded verbatim in the summary, and no repository file has been modified.</done>
  <resume-signal>Type "approved" to authorize the recharts install, or "rejected" with your reasoning.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Confirm or create the first Supabase Auth user (D-19)</name>
  <read_first>
    - `.planning/phases/05-admin-backend/05-CONTEXT.md` — D-19 and the `## ⚠ Blocker affecting this phase — RESOLVED, with one narrower gap` section
    - `.planning/phases/05-admin-backend/05-RESEARCH.md` — `## Open Questions` item 1 and the `## Environment Availability` table row "At least one `auth.users` row" (marked `✗ unverified`)
    - `.planning/phases/01-foundation/01-CONTEXT.md` — D-12, which assigned first-admin creation to the owner via the Supabase dashboard
    - `F:/2026/Projects/Windshield/src/lib/supabase/middleware.ts` — note lines 8-12: when `NEXT_PUBLIC_SUPABASE_URL` or `NEXT_PUBLIC_SUPABASE_ANON_KEY` is absent the middleware returns `NextResponse.next()` and skips auth entirely, so any verification of the redirect behavior must run with `.env.local` actually loaded
  </read_first>
  <action>
    This is a genuine human-only action: creating a Supabase Auth user requires an authenticated session in the Supabase web dashboard for project ref `kyhvgskeihtccylpdkas`. There is no CLI or API path available here — `createAdminClient()` could technically call `auth.admin.createUser()`, but no admin UI exists yet to invoke it from and bootstrapping the first credential through generated code would put a real password into a repo-tracked execution log.

    Before pausing, do the parts that are automatable: confirm `.env.local` exists at the repo root and contains non-placeholder values for `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` and `SUPABASE_SERVICE_ROLE_KEY` — report which of the three are present, and report ONLY presence and key-name, never the value. Do not print, echo, or log any key material.

    Then ask the developer to confirm the `auth.users` row count and record the answer in the summary. Record only the count and whether a new user was created — never the email/password pair, and never the password in any form.
  </action>
  <what-built>
    Nothing built. The login code does not exist yet — this task establishes the account that login code will be tested against.

    Why this blocks verification rather than construction: D-05 makes `auth.users` the entire admin list. Every one of the phase's five success criteria depends on being able to hold a session:
    - Criterion 2 ("logging in with valid email/password lands on the dashboard") needs valid credentials to exist.
    - Criteria 3 and 4 (dashboard cards, charts, tables) render behind the middleware guard, reachable only with a session.
    - Criterion 5 (add and remove an admin) needs a caller identity for D-10's self-delete guard, and needs at least two accounts to exist before a removal can be attempted without tripping D-10's last-admin guard.

    Phase 4's `04-02-SUMMARY.md` proved the live Supabase project, the pushed migration, and enforced RLS with dated HTTP-status and Postgres-error-code evidence (including a real `23505` constraint violation), so the database itself is confirmed. `auth.users` is the one table nothing in this repo has ever verified.
  </what-built>
  <how-to-verify>
    1. Open the Supabase dashboard for project ref `kyhvgskeihtccylpdkas` and go to **Authentication -> Users**.
    2. Report the number of rows listed.
    3. If the count is 0: click **Add user -> Create new user**, supply an email you control and a password of at least 8 characters, and enable **Auto Confirm User** (with no SMTP configured — D-08 — an unconfirmed user cannot complete the confirmation step and will not be able to sign in).
    4. If the count is 1: note that D-10's last-admin guard will refuse to remove it, which is correct behavior. Success criterion 5's removal step will be exercised against a second account created through `/admin/users` itself in plan `05-08`.
    5. Confirm you have the email and password recorded somewhere outside this repository — there is no password-reset flow (D-08) and no email delivery, so a lost credential is only recoverable from the Supabase dashboard.
    6. Report back the row count and whether you created a new user. Do not paste the password anywhere in this conversation.
  </how-to-verify>
  <acceptance_criteria>
    - The summary records the confirmed `auth.users` row count as a number, and that count is >= 1
    - The summary records whether a new user was created during this task
    - The summary contains no password, no email/password pair, and no key material
    - The summary records which of `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` are present in `.env.local`, by name only
    - `git status --porcelain` output is unchanged from before the plan started
  </acceptance_criteria>
  <done>The developer has confirmed at least one `auth.users` row exists in project `kyhvgskeihtccylpdkas`, the count is recorded in the summary, no credential material was written anywhere, and no repository file changed.</done>
  <resume-signal>Report the user count (e.g. "1 user exists" or "created 1 user, now 1"). Do not include the password.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local `node_modules` → production bundle | Third-party code executes at install time and ships to the browser |
| shadcn registry (`ui.shadcn.com/r/...`) → `src/components/ui/*.tsx` | Fetched source is written directly into the repo and compiled into the app |
| Developer → Supabase dashboard | A real credential is minted outside version control; it must never enter the repo, a log, or a summary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | `npm install recharts` (via `npx shadcn@latest add chart`) | mitigate | slopcheck verdict `[OK]` recorded in `05-RESEARCH.md`; this blocking human checkpoint plus npmjs.com metadata verification (repo URL `github.com/recharts/recharts`, download volume, exact spelling) is the control before any install runs |
| T-05-SC-02 | Tampering | `npx shadcn@latest add chart alert-dialog table` | mitigate | The CLI writes reviewable source into `src/components/ui/` rather than adding opaque dependencies; plan `05-02` greps each generated file for `@radix-ui` imports and unexpected new imports before any consumer is written |
| T-05-01 | Information Disclosure | First-admin credential created in the Supabase dashboard | mitigate | Task 2 forbids the password entering the conversation, the summary, or any log; only the row count is recorded. Env-var verification reports key names and presence only, never values |
| T-05-02 | Spoofing | Zero `auth.users` rows leaving `/admin/*` effectively unreachable and untestable | mitigate | Task 2 blocks the phase until >= 1 row is confirmed, so the middleware guard is verified against a real session rather than assumed |
</threat_model>

<verification>
Human has explicitly approved or rejected `recharts`. Human has confirmed the `auth.users` row count is >= 1. No repository files changed by either task.
</verification>

<success_criteria>
`git status --porcelain` shows no new or modified files attributable to this plan; the `recharts` approval decision is recorded verbatim in the summary; the `auth.users` row count is recorded as a number >= 1; no credential or key value appears anywhere in the summary.
</success_criteria>

<output>
Create `.planning/phases/05-admin-backend/05-01-SUMMARY.md` when done. Plan `05-02` reads the recharts decision before running `npx shadcn@latest add chart`; plan `05-09` reads the user count before verifying success criteria 2 and 5.
</output>
