---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - .gitignore
autonomous: false
requirements:
  - FDN-06
user_setup:
  - service: vercel
    why: "Hosting and deployment"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Same as .env.local — Supabase Dashboard -> Project Settings -> API"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Same as .env.local — Supabase Dashboard -> Project Settings -> API"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Same as .env.local — Supabase Dashboard -> Project Settings -> API"
      - name: NEXT_PUBLIC_SITE_URL
        source: "Vercel assigns this after first deploy — use the .vercel.app domain"
    dashboard_config:
      - task: "Create Vercel account (if not already)"
        location: "vercel.com"
      - task: "Connect GitHub repository to Vercel"
        location: "Vercel Dashboard -> New Project -> Import from GitHub"
  - service: github
    why: "Source control and Vercel CI/CD trigger"
    dashboard_config:
      - task: "Create GitHub repository for the project"
        location: "github.com -> New repository"
must_haves:
  truths:
    - "Code is pushed to a GitHub repository"
    - "Vercel deployment succeeds with a live URL"
    - "The deployed site shows the TopNav, Footer, and brand colors"
    - "Environment variables are configured in Vercel for production"
  artifacts:
    - path: ".gitignore"
      provides: "Complete gitignore covering Next.js, env, Vercel, Supabase"
      contains: ".vercel"
  key_links:
    - from: "GitHub repository"
      to: "Vercel project"
      via: "GitHub integration auto-deploys on push"
      pattern: "N/A — external service link"
---

<objective>
Push the project to GitHub, connect it to Vercel, configure environment variables, and verify the first successful deployment with the navigation shell visible at a live URL.

Purpose: Establishes the deployment pipeline so every subsequent phase automatically deploys. FDN-06 requires a working Vercel deployment with a passing build.
Output: A live Vercel URL where the site renders correctly with TopNav, Footer, and brand theming.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize git, finalize .gitignore, and prepare for GitHub push</name>
  <files>.gitignore</files>
  <read_first>.planning/phases/01-foundation/01-RESEARCH.md, .gitignore</read_first>
  <action>
1. Ensure `.gitignore` is complete with all necessary entries. Read the current file and make sure it includes at minimum:
```
# Dependencies
node_modules/

# Next.js
.next/
out/

# Environment
.env.local
.env*.local

# Vercel
.vercel/

# Supabase
supabase/.temp/
supabase/.branches/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
```

2. Initialize git repository if not already initialized:
```bash
git init
```

3. Stage all project files and make the initial commit:
```bash
git add -A
git commit -m "feat: Phase 1 foundation — Next.js 15 scaffold, Tailwind v4 brand theme, shadcn/ui, Supabase SSR, database schema, navigation shell"
```

4. Verify `.env.local` is NOT staged:
```bash
git status
```
Confirm `.env.local` does not appear in the tracked files. If it does, remove it:
```bash
git rm --cached .env.local
```

5. Verify no secrets are in the commit — specifically check that no real Supabase keys appear:
```bash
git diff --cached --name-only
```
This should show source files, config files, .env.example (safe), but NOT .env.local.

6. Create a GitHub repository. Use the GitHub CLI if available:
```bash
gh repo create alamo-windshield --public --source=. --remote=origin --push
```

If `gh` CLI is not available, provide instructions for the user to create the repo manually and push:
```bash
git remote add origin https://github.com/USERNAME/alamo-windshield.git
git branch -M main
git push -u origin main
```
  </action>
  <verify>
    <automated>git log --oneline -1 2>&1 && git status --short 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains `.env.local` and `.env*.local`
    - .gitignore contains `.vercel/`
    - .gitignore contains `supabase/.temp/`
    - .gitignore contains `node_modules/`
    - .gitignore contains `.next/`
    - Git repository is initialized with at least one commit
    - .env.local is NOT tracked by git
    - No file containing real Supabase keys is committed
  </acceptance_criteria>
  <done>Git initialized, .gitignore complete, initial commit made, code pushed to GitHub (or ready to push).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Connect Vercel, configure env vars, verify deployment</name>
  <files>.vercel/project.json</files>
  <action>
The executor should attempt to deploy via Vercel CLI if available. If CLI auth fails, pause for user to complete via Vercel dashboard.

Step 1 — Vercel Setup:
1. Go to vercel.com and log in (or create account)
2. Click "New Project" then "Import Git Repository"
3. Select the GitHub repo that was just pushed
4. Framework will auto-detect as Next.js

Step 2 — Environment Variables (BEFORE deploying):
Add these in Vercel's Environment Variables section:
- `NEXT_PUBLIC_SUPABASE_URL` = Supabase project URL (Production + Preview)
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` = anon key (Production + Preview)
- `SUPABASE_SERVICE_ROLE_KEY` = service role key (Production ONLY — not Preview, to reduce exposure)
- `NEXT_PUBLIC_SITE_URL` = leave blank for now, update after first deploy with the .vercel.app URL

Step 3 — Deploy:
Click "Deploy" and wait for the build to complete.

Step 4 — Verify at the live URL:
1. Visit the Vercel URL (e.g., alamo-windshield.vercel.app)
2. Confirm the home page renders with TopNav (Home, About, Contact, Admin links), phone number (210) 555-0100, brand red color (#B91C1C), and Footer with business hours and San Antonio TX
3. Navigate to /about — confirm TopNav + Footer present
4. Navigate to /contact — confirm TopNav + Footer present
5. Navigate to /admin/login — confirm it shows the login placeholder WITHOUT TopNav/Footer
6. On mobile viewport (resize browser or use DevTools): confirm hamburger menu appears and opens a drawer with nav links
7. Check Vercel build logs — confirm zero errors

Step 5 — Update NEXT_PUBLIC_SITE_URL:
After successful deploy, go back to Vercel Environment Variables and set `NEXT_PUBLIC_SITE_URL` to the Vercel domain (e.g., `https://alamo-windshield.vercel.app`). Trigger a redeploy.
  </action>
  <verify>
    <automated>echo "Checkpoint: user verifies Vercel deployment at live URL"</automated>
  </verify>
  <acceptance_criteria>
    - Vercel build completes with zero errors
    - Live URL returns HTTP 200 on /, /about, /contact, /admin/login
    - TopNav and Footer visible on public pages
    - Brand red (#B91C1C) applied to primary elements
    - Mobile hamburger menu functional
    - Environment variables configured in Vercel dashboard
  </acceptance_criteria>
  <done>Vercel deployment live with passing build, all routes render correctly, env vars configured, deployment pipeline established for future phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub repo <-> public internet | Repo may be public; ensure no secrets committed |
| Vercel env vars <-> build | NEXT_PUBLIC_ vars embedded at build time in client bundle |
| Vercel preview deploys | Service role key restricted to Production only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | I (Information Disclosure) | .env.local in git | mitigate | .gitignore excludes .env.local. Task 1 verifies it is not tracked. Pre-commit check: `git diff --cached` must not contain real keys. |
| T-01-15 | I (Information Disclosure) | SUPABASE_SERVICE_ROLE_KEY on Vercel | mitigate | Set service role key for Production environment ONLY (not Preview). This limits exposure from preview deployment URLs. |
| T-01-16 | I (Information Disclosure) | NEXT_PUBLIC_ vars in client bundle | accept | NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY are designed to be public. RLS enforces actual access control. This is the intended Supabase architecture. |
</threat_model>

<verification>
1. GitHub repository exists with the project code
2. Vercel deployment URL returns a working page (HTTP 200)
3. Build log shows zero errors
4. All four routes (/, /about, /contact, /admin/login) render correctly at the live URL
5. TopNav + Footer visible on public pages, absent on admin login
6. Brand red color visible on primary elements
7. Mobile hamburger menu works at the live URL
</verification>

<success_criteria>
- Code pushed to GitHub repository
- Vercel deployment succeeds with live URL (FDN-06)
- Environment variables configured in Vercel dashboard
- Navigation shell (TopNav + Footer) renders at live URL on all screen sizes
- No secrets committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
