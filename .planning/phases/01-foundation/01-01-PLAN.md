---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - src/lib/utils.ts
  - next.config.ts
  - .gitignore
autonomous: true
requirements:
  - FDN-01
  - FDN-02
  - FDN-03
must_haves:
  truths:
    - "npm run dev serves a page on localhost:3000 without errors"
    - "Brand red #B91C1C is the primary color applied via bg-primary and text-primary utilities"
    - "Inter body font and Space Grotesk display font render correctly"
    - "shadcn Button component renders with brand-red styling"
  artifacts:
    - path: "package.json"
      provides: "Next.js 15, React 19, Tailwind v4, Supabase packages"
      contains: "next"
    - path: "src/app/globals.css"
      provides: "Brand color tokens in OKLCH and @theme block"
      contains: "@theme inline"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Inter + Space Grotesk font variables on html"
      contains: "font-inter"
    - path: "src/components/ui/button.tsx"
      provides: "shadcn Button primitive"
      contains: "Button"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import './globals.css'"
      pattern: "globals\\.css"
    - from: "src/app/globals.css"
      to: "Tailwind utilities"
      via: "@theme inline block exposes --color-primary"
      pattern: "--color-primary"
---

<objective>
Scaffold the Next.js 15 project with TypeScript, configure Tailwind v4 with the white/red/black brand palette, set up fonts via next/font, and initialize shadcn/ui with base components.

Purpose: Establishes the project skeleton that every subsequent plan builds on. Nothing else can proceed without a working Next.js project with the correct Tailwind theme.
Output: A running Next.js dev server with brand-themed shadcn components ready to use.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project with Tailwind v4 brand theme and fonts</name>
  <files>package.json, tsconfig.json, src/app/layout.tsx, src/app/globals.css, src/app/page.tsx, next.config.ts, .gitignore</files>
  <read_first>.planning/phases/01-foundation/01-RESEARCH.md, .planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>
1. Run the scaffold command (per D-16 for src/ directory, D-18 for @/ alias):
```bash
npx create-next-app@15 . --typescript --tailwind --app --src-dir --import-alias "@/*" --no-git
```

2. Verify package.json shows `"next": "^15.x.x"` (NOT 16.x — Pitfall 1 from research). If version is 16.x, the wrong scaffold was used; re-run with `@15`.

3. Replace the contents of `src/app/globals.css` with the exact Tailwind v4 CSS-first configuration (per D-01, D-05). Use OKLCH values for #B91C1C:
```css
@import "tailwindcss";

@custom-variant dark (&:is(.dark *));

:root {
  --background: oklch(1 0 0);
  --foreground: oklch(0.145 0 0);
  --primary: oklch(0.505 0.213 27.518);
  --primary-foreground: oklch(0.985 0 0);
  --destructive: oklch(0.505 0.213 27.518);
  --destructive-foreground: oklch(0.985 0 0);
  --muted: oklch(0.97 0 0);
  --muted-foreground: oklch(0.556 0 0);
  --border: oklch(0.922 0 0);
  --ring: oklch(0.505 0.213 27.518);
  --radius: 0.375rem;
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.145 0 0);
  --popover: oklch(1 0 0);
  --popover-foreground: oklch(0.145 0 0);
  --secondary: oklch(0.97 0 0);
  --secondary-foreground: oklch(0.205 0 0);
  --accent: oklch(0.97 0 0);
  --accent-foreground: oklch(0.205 0 0);
  --input: oklch(0.922 0 0);
  --sidebar-background: oklch(0.985 0 0);
  --sidebar-foreground: oklch(0.145 0 0);
  --sidebar-primary: oklch(0.505 0.213 27.518);
  --sidebar-primary-foreground: oklch(0.985 0 0);
  --sidebar-accent: oklch(0.97 0 0);
  --sidebar-accent-foreground: oklch(0.205 0 0);
  --sidebar-border: oklch(0.922 0 0);
  --sidebar-ring: oklch(0.505 0.213 27.518);
  --chart-1: oklch(0.505 0.213 27.518);
  --chart-2: oklch(0.145 0 0);
  --chart-3: oklch(0.556 0 0);
  --chart-4: oklch(0.7 0.1 27.518);
  --chart-5: oklch(0.8 0.05 27.518);
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-destructive: var(--destructive);
  --color-destructive-foreground: var(--destructive-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-border: var(--border);
  --color-ring: var(--ring);
  --color-card: var(--card);
  --color-card-foreground: var(--card-foreground);
  --color-popover: var(--popover);
  --color-popover-foreground: var(--popover-foreground);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-input: var(--input);
  --color-sidebar-background: var(--sidebar-background);
  --color-sidebar-foreground: var(--sidebar-foreground);
  --color-sidebar-primary: var(--sidebar-primary);
  --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
  --color-sidebar-accent: var(--sidebar-accent);
  --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
  --color-sidebar-border: var(--sidebar-border);
  --color-sidebar-ring: var(--sidebar-ring);
  --color-chart-1: var(--chart-1);
  --color-chart-2: var(--chart-2);
  --color-chart-3: var(--chart-3);
  --color-chart-4: var(--chart-4);
  --color-chart-5: var(--chart-5);
  --radius-sm: calc(var(--radius) - 4px);
  --radius-md: calc(var(--radius));
  --radius-lg: calc(var(--radius) + 4px);
  --radius-xl: calc(var(--radius) + 8px);
  --font-sans: var(--font-inter), ui-sans-serif, system-ui, sans-serif;
  --font-display: var(--font-space-grotesk), ui-sans-serif, system-ui, sans-serif;
}
```

IMPORTANT: Do NOT create a `tailwind.config.js` or `tailwind.config.ts` file. Tailwind v4 is CSS-first — the `@theme` block in globals.css replaces the config file entirely (Pitfall from research).

4. Update `src/app/layout.tsx` to load Inter + Space Grotesk (per D-02). Font variables go on `<html>` NOT `<body>` (Pitfall from research):
```tsx
import type { Metadata } from 'next'
import { Inter, Space_Grotesk } from 'next/font/google'
import './globals.css'

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
})

const spaceGrotesk = Space_Grotesk({
  subsets: ['latin'],
  variable: '--font-space-grotesk',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'Alamo City Windshield Repair',
  description: 'Professional windshield repair and replacement in San Antonio, TX',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={`${inter.variable} ${spaceGrotesk.variable}`}>
      <body className="font-sans antialiased bg-background text-foreground">
        {children}
      </body>
    </html>
  )
}
```

5. Replace `src/app/page.tsx` with a minimal placeholder that uses brand colors to verify theming:
```tsx
export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-24">
      <h1 className="font-display text-4xl font-bold text-foreground">
        Alamo City <span className="text-primary">Windshield Repair</span>
      </h1>
      <p className="mt-4 text-muted-foreground">Foundation scaffold — Phase 1</p>
    </main>
  )
}
```

6. Ensure `.gitignore` includes:
```
node_modules/
.next/
.env.local
.env*.local
.vercel/
```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"next"` with version starting with `15.` (NOT 16.x)
    - src/app/globals.css contains `@import "tailwindcss"` (Tailwind v4 CSS import)
    - src/app/globals.css contains `--primary: oklch(0.505 0.213 27.518)` (brand red)
    - src/app/globals.css contains `@theme inline` block with `--color-primary`
    - src/app/layout.tsx contains `--font-inter` and `--font-space-grotesk`
    - src/app/layout.tsx font variables applied on `html` element (not body)
    - NO `tailwind.config.js` or `tailwind.config.ts` file exists
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Next.js 15 project builds successfully with Tailwind v4 brand theme (red #B91C1C), Inter + Space Grotesk fonts, and correct OKLCH color tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and install Phase 1 components</name>
  <files>src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/separator.tsx, src/components/ui/sheet.tsx, src/lib/utils.ts, components.json</files>
  <read_first>src/app/globals.css, package.json, .planning/phases/01-foundation/01-RESEARCH.md</read_first>
  <action>
1. Initialize shadcn/ui (accepts defaults which selects new-york style for Tailwind v4):
```bash
npx shadcn@latest init -d
```
If the `-d` flag does not work or hangs, run interactively and choose: style=new-york, base color=neutral, CSS variables=yes.

IMPORTANT: After init, check that shadcn did NOT overwrite `globals.css` with default colors. If it did, restore the brand theme from Task 1 (the OKLCH tokens with `--primary: oklch(0.505 0.213 27.518)`). shadcn init may try to write its own color tokens — our brand colors take precedence (per D-01, D-05).

Also check that shadcn did NOT create a `tailwind.config.js` or `tailwind.config.ts`. If it did, delete it — Tailwind v4 does not use a config file.

2. Install Phase 1 shadcn components (for nav, footer, mobile menu):
```bash
npx shadcn@latest add button card separator sheet
```

3. Verify `src/lib/utils.ts` exists with the `cn()` helper (shadcn creates this automatically). It should contain:
```typescript
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

4. Verify `components.json` exists at the project root. It should reference `src/components/ui` as the component path.

5. Update `src/app/page.tsx` to include a Button to visually verify shadcn theming:
```tsx
import { Button } from '@/components/ui/button'

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-6 p-24">
      <h1 className="font-display text-4xl font-bold text-foreground">
        Alamo City <span className="text-primary">Windshield Repair</span>
      </h1>
      <p className="text-muted-foreground">Foundation scaffold — Phase 1</p>
      <div className="flex gap-4">
        <Button>Get Estimate</Button>
        <Button variant="outline">Contact Us</Button>
      </div>
    </main>
  )
}
```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - components.json exists at project root
    - src/components/ui/button.tsx exists
    - src/components/ui/card.tsx exists
    - src/components/ui/separator.tsx exists
    - src/components/ui/sheet.tsx exists
    - src/lib/utils.ts contains `cn` function export
    - src/app/globals.css still contains `--primary: oklch(0.505 0.213 27.518)` (not overwritten by shadcn init)
    - NO `tailwind.config.js` or `tailwind.config.ts` exists
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>shadcn/ui initialized with new-york style, Button/Card/Separator/Sheet components installed, brand theme preserved, build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build artifacts | Ensure no secrets leak into client bundle at scaffold time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I (Information Disclosure) | globals.css | accept | No secrets in CSS; brand colors are public. Low risk. |
| T-01-02 | T (Tampering) | package.json | mitigate | Pin Next.js to v15 major (not @latest) to prevent scaffold pulling v16. Verify version after scaffold. |
</threat_model>

<verification>
1. `npm run build` completes with exit code 0
2. `npx tsc --noEmit` shows zero type errors
3. `npm run dev` serves a page with brand-red "Windshield Repair" text and a red Button
4. No `tailwind.config.js` file exists (Tailwind v4 CSS-first)
5. `grep "oklch(0.505 0.213 27.518)" src/app/globals.css` returns matches for --primary
</verification>

<success_criteria>
- Next.js 15 project scaffolded with src/ directory, TypeScript, Tailwind v4
- Brand color palette (#B91C1C / oklch(0.505 0.213 27.518)) configured as --primary
- Inter (body) and Space Grotesk (display) fonts loaded via next/font with CSS variables
- shadcn/ui initialized with Button, Card, Separator, Sheet components
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
