---
phase: 04-booking-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [BOOK-01, CONT-06]
user_setup: []

must_haves:
  truths:
    - "A human has explicitly approved installing all five npm packages before any of them is added to package.json"
  artifacts: []
  key_links: []
---

<objective>
Clear the package legitimacy gate for the five npm packages this phase adds, before any install task runs.

Purpose: This phase cannot be built with the current dependency set. `04-RESEARCH.md` audited two packages (`react-day-picker`, `date-fns` — both `[OK]`), but `04-PATTERNS.md` verified directly against `package.json` that **three more** are also missing, contradicting both CONTEXT.md and RESEARCH.md which claim they are "already installed": `react-hook-form`, `zod`, and `@hookform/resolvers`. All five must clear the same gate. This mirrors plan `03-01`, which existed solely to clear two packages with the owner before any install ran.

Output: Recorded human approval (or rejection) of the five package installs. No files change in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-booking-contact/04-RESEARCH.md
@.planning/phases/04-booking-contact/04-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve npm installs for the five phase-4 packages</name>
  <read_first>
    - `.planning/phases/04-booking-contact/04-RESEARCH.md` — read the `## Package Legitimacy Audit` section (covers `react-day-picker` and `date-fns` only, both `[OK]`) and the `## Standard Stack` tables
    - `.planning/phases/04-booking-contact/04-PATTERNS.md` — read the `## ⚠ Dependency correction (read before planning)` section at the top, which lists all five packages and flags the three that RESEARCH.md/CONTEXT.md wrongly claim are installed
    - `F:/2026/Projects/Windshield/package.json` — confirm directly that none of the five packages appear in `dependencies` or `devDependencies`
  </read_first>
  <action>
    Do not install anything in this plan. Present the five proposed packages to the developer with the evidence below, wait for an explicit decision, and record that decision verbatim in the plan summary.

    Before presenting, re-verify the claim that all five are absent by reading `package.json` — the currently installed set relevant to this phase is `@base-ui/react`, `@supabase/ssr`, `@supabase/supabase-js`, `class-variance-authority`, `clsx`, `lucide-react`, `motion`, `next`, `react`, `react-dom`, `server-only`, `shadcn`, `tailwind-merge`, `tw-animate-css` plus dev deps including `vitest`. If any of the five IS in fact already present, say so explicitly rather than asking for approval to install it again.

    Per the package legitimacy protocol, a legitimacy checkpoint is never auto-approvable — `workflow.auto_advance` does not apply and this checkpoint must block on a real human response.
  </action>
  <what-built>
    Nothing has been installed yet. This is a pre-install gate.

    Five packages are proposed, all named in this project's own `CLAUDE.md` recommended stack:

    **1. `react-day-picker@^10.0.1`** — production dependency.
    - Purpose: the calendar month grid behind BOOK-01. It is the library the shadcn `Calendar` component wraps; the generated `src/components/ui/calendar.tsx` is a thin wrapper around it.
    - Audit status: **audited in `04-RESEARCH.md`, verdict `[OK]` / Approved.** Source repo `github.com/gpbl/react-day-picker`, long-standing (v8 line since ~2023), latest `10.0.1` published 2026-05-15.
    - Note: CONTEXT.md assumed v9. RESEARCH.md verified live against the npm registry that `@latest` now resolves to **v10.0.1**. The APIs this phase uses (`mode`, `selected`/`onSelect`, `disabled`, `modifiers`, `month`/`onMonthChange`) are unchanged between v9 and v10; only deprecated props (`fromDate`, `toDate`, `initialFocus`) were removed, none of which this phase needs.

    **2. `date-fns@^4.4.0`** — production dependency.
    - Purpose: sibling/peer dependency the shadcn Calendar registry entry declares; used for date arithmetic (the 30-day booking window, ISO date-string formatting).
    - Audit status: **audited in `04-RESEARCH.md`, verdict `[OK]` / Approved.** slopcheck noted "no source repository linked" in the npm metadata — RESEARCH.md cross-verified this independently as a known cosmetic gap in `date-fns`'s own `package.json`, not a legitimacy signal. `date-fns` is one of the most widely-used JS date libraries.

    **3. `react-hook-form@^7`** — production dependency.
    - Purpose: form state for both the booking form and the contact form. **CONT-06 names this library by requirement ID** ("Form validation with Zod + react-hook-form").
    - Audit status: **NOT in the RESEARCH.md audit table** — RESEARCH.md wrongly assumed it was already installed, so it never audited it. Maintained by `react-hook-form` org, repo `github.com/react-hook-form/react-hook-form`, one of the most-downloaded React form libraries.

    **4. `zod@^4`** — production dependency.
    - Purpose: the shared validation schema for both forms, run client-side via the resolver AND re-run server-side inside each Server Action as the untrusted-input gate. **CONT-06 names this library by requirement ID.** It is also the mechanism behind threat mitigation for V5 Input Validation.
    - Audit status: **NOT in the RESEARCH.md audit table** — same wrong assumption as above. Repo `github.com/colinhacks/zod`.

    **5. `@hookform/resolvers@^3`** — production dependency.
    - Purpose: the bridge that lets a Zod schema be passed as `useForm({ resolver: zodResolver(schema) })`. Required to combine packages 3 and 4; there is no way to satisfy CONT-06 with react-hook-form + zod without it.
    - Audit status: **NOT in the RESEARCH.md audit table** — same wrong assumption. Published by the same `react-hook-form` org as package 3.

    **Why three of the five were never audited:** both `04-CONTEXT.md` and `04-RESEARCH.md` state that `react-hook-form`, `zod`, and `@hookform/resolvers` are "already installed." `04-PATTERNS.md` checked `package.json` directly and found this is false. Because research believed they were present, it never ran them through the legitimacy protocol — so three of these five carry no slopcheck verdict at all and need your eyes on the registry listings.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/react-day-picker and confirm the repository links to `github.com/gpbl/react-day-picker` and the latest version is in the 10.x line.
    2. Open https://www.npmjs.com/package/date-fns and confirm it shows multi-million weekly downloads and is the well-known date utility library.
    3. Open https://www.npmjs.com/package/react-hook-form and confirm the repository links to `github.com/react-hook-form/react-hook-form`.
    4. Open https://www.npmjs.com/package/zod and confirm the repository links to `github.com/colinhacks/zod`.
    5. Open https://www.npmjs.com/package/@hookform/resolvers and confirm it is published under the `@hookform` scope by the same maintainers as react-hook-form.
    6. Confirm the spelling in each proposed install command is exact — not `react-daypicker`, `datefns`, `react-hookform`, `zod-js`, or `@hookforms/resolvers`:
       - `npm install react-day-picker date-fns react-hook-form zod @hookform/resolvers`
    7. Confirm you accept that plan `04-04` will additionally run `npx shadcn@latest add calendar` and `npx shadcn@latest add form`, which write component source files into `src/components/ui/` rather than adding runtime dependencies.
  </how-to-verify>
  <acceptance_criteria>
    - Human has responded with an explicit approval or rejection decision covering all five packages
    - The summary records which of the five were approved, verbatim, including any partial approval
    - If any package is rejected: plan `04-04` must not install it, and the summary states which requirement (BOOK-01 for react-day-picker/date-fns, CONT-06 for react-hook-form/zod/@hookform/resolvers) is consequently blocked
    - No file in the repository has been modified by this plan — `git status --porcelain` output is unchanged from before the plan started
  </acceptance_criteria>
  <done>The developer has explicitly approved or rejected all five packages, the decision is recorded verbatim in the summary, and no repository file has been modified.</done>
  <resume-signal>Type "approved" to authorize all five installs, or name the specific packages you approve/reject.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local `node_modules` | Third-party code executes at install time and ships into the deployed production bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | `npm install react-day-picker date-fns react-hook-form zod @hookform/resolvers` | mitigate | slopcheck run during research for `react-day-picker` and `date-fns` (both `[OK]`); the remaining three carry no audit because research wrongly assumed them installed, so this blocking human checkpoint plus registry metadata verification (repo URL, maintainer, download volume) on npmjs.com is the control before any install |
| T-04-SC-02 | Tampering | `npx shadcn@latest add calendar` / `add form` | mitigate | shadcn CLI writes source files into the repo rather than adding an opaque dependency; the generated `calendar.tsx` is reviewed and patched in plan `04-04` (RESEARCH.md Pitfall 1) so its contents are human-visible before use |
</threat_model>

<verification>
Human has explicitly approved or rejected the five proposed npm installs. No repository files changed.
</verification>

<success_criteria>
`git status --porcelain` shows no new or modified files attributable to this plan, and the human's approval decision for all five packages is recorded in the plan summary.
</success_criteria>

<output>
Create `.planning/phases/04-booking-contact/04-01-SUMMARY.md` when done. Record the human's verbatim decision — plan `04-04` reads it before installing.
</output>
</content>
</invoke>
