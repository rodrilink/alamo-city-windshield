---
phase: 03-vin-estimate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [VIN-04]
user_setup: []

must_haves:
  truths:
    - "A human has explicitly approved installing `server-only` and `vitest` before either is added to package.json"
  artifacts: []
  key_links: []
---

<objective>
Clear the package legitimacy gate for the two npm packages this phase adds, before any install task runs.

Purpose: `03-RESEARCH.md` audited exactly one package (`server-only`, verdict `[OK]`). Planning discovered that the research's `node --test` recommendation does not work in this repository, so `vitest` is required instead — and `vitest` is NOT in the audited table. Running `slopcheck install vitest` during planning returned **`[SUS]` — "Suspiciously close to 'vite'. Could be a typosquat."** Per the package legitimacy protocol, a `[SUS]` verdict requires a blocking human confirmation that is never auto-approvable, regardless of `workflow.auto_advance`.

Output: Recorded human approval (or rejection) of the two package installs. No files change in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-vin-estimate/03-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve npm installs for `server-only` and `vitest`</name>
  <read_first>
    - `.planning/phases/03-vin-estimate/03-RESEARCH.md` — read the `## Package Legitimacy Audit` section (covers `server-only` only) and the `## Standard Stack` §Supporting table
    - `F:/2026/Projects/Windshield/package.json` — confirm neither package is currently present in `dependencies` or `devDependencies`
  </read_first>
  <action>
    Do not install anything in this plan. Present the two proposed packages to the developer with the evidence below, wait for an explicit decision, and record that decision verbatim in the plan summary. `workflow.auto_advance` does not apply — a `[SUS]` slopcheck verdict is never auto-approvable, so this checkpoint must block on a real human response.
  </action>
  <what-built>
    Nothing has been installed yet. This is a pre-install gate.

    Two packages are proposed:

    **1. `server-only@0.0.1`** — production dependency.
    - Purpose: makes `import 'server-only'` the first line of `src/lib/pricing.ts`, `src/lib/vin.ts`, `src/lib/vin-cache.ts`, and `src/lib/supabase/admin.ts`, so Next.js raises a **build error** if any Client Component transitively imports them. This is the build-time enforcement of decision D-15 ("the pricing formula never ships to the browser") and of the service-role-key containment requirement.
    - Audit status: **already audited in `03-RESEARCH.md` with verdict `[OK]` / Approved.** Maintainer `sebmarkbage` (React core team), published from the `facebook/react` repo, 13.2M weekly downloads, 3 files / 611 bytes unpacked, empty `postinstall`.

    **2. `vitest@4.1.10`** — dev dependency.
    - Purpose: run `src/lib/pricing.test.ts` (the six locked D-06 fixtures — the phase's only objective proof the pricing formula is correct) and `src/lib/vin.test.ts` (the D-17/D-18/D-19 classifier).
    - **NOT in the research audit table.** Research recommended Node's built-in `node --test` instead, but that recommendation was empirically disproven during planning: this repo's `package.json` has no `"type": "module"`, so `node --test` loads `.ts` test files as CommonJS and fails with `SyntaxError: Cannot use import statement outside a module`; and `import 'server-only'` resolves to `node_modules/server-only/index.js`, which **unconditionally throws** under plain Node (verified: the file's entire body is a `throw new Error(...)`). Vitest with a `resolve.alias` stub for `server-only` was verified working — all six D-06 fixtures pass.
    - **slopcheck verdict: `[SUS]`** — the only reason: *"Suspiciously close to 'vite'. Could be a typosquat. Did you mean: vite"*. This is a name-similarity heuristic firing on Vitest's real, intentional relationship to Vite (Vitest is Vite's official test runner, maintained by the Vite team). It is not evidence of a typosquat, but the `[SUS]` tier requires your explicit sign-off.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/vitest and confirm:
       - Repository links to `github.com/vitest-dev/vitest`
       - Weekly downloads are in the tens of millions (planning measured 88,317,116 for the week ending 2026-08-03)
       - First published 2021-12-03, latest version 4.1.10
       - It is described as "Next generation testing framework powered by Vite"
    2. Open https://www.npmjs.com/package/server-only and confirm the maintainer is `sebmarkbage` and the homepage is `reactjs.org`.
    3. Confirm the spelling in both proposed install commands is exactly `vitest` (v-i-t-e-s-t) and `server-only` — not `vite-test`, `vi-test`, `serveronly`, or any variant:
       - `npm install server-only`
       - `npm install -D vitest`
    4. Confirm you accept adding a test runner to this project. Vitest brings ~44 transitive packages into `devDependencies` only; it does not affect the production bundle or the Vercel build output.
  </how-to-verify>
  <acceptance_criteria>
    - Human has responded with an explicit approval or rejection decision for both packages
    - If rejected: plan `03-02` must not run its install task, and the phase is blocked pending an alternative testing approach chosen by the developer
    - No file in the repository has been modified by this plan (`git status --porcelain` output is unchanged from before the plan started)
  </acceptance_criteria>
  <done>The developer has explicitly approved or rejected `server-only` and `vitest`, the decision is recorded verbatim in the summary, and no repository file has been modified.</done>
  <resume-signal>Type "approved" to authorize both installs, "approve server-only only" to authorize just the production dependency, or describe your objection.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local `node_modules` | Third-party code executes at install time and ships into the deployed bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | `npm install server-only`, `npm install -D vitest` | mitigate | slopcheck run during planning (`server-only` `[OK]`, `vitest` `[SUS]` typosquat-similarity false positive) plus this blocking human checkpoint; registry metadata (maintainer, repo URL, download volume, empty `postinstall`) verified against npmjs.com by the human before install |
</threat_model>

<verification>
Human has explicitly approved or rejected the two proposed npm installs. No repository files changed.
</verification>

<success_criteria>
`git status --porcelain` shows no new or modified files attributable to this plan, and the human's approval decision for `server-only` and `vitest` is recorded in the plan summary.
</success_criteria>

<output>
Create `.planning/phases/03-vin-estimate/03-01-SUMMARY.md` when done. Record the human's verbatim decision — plan `03-02` reads it before installing.
</output>
