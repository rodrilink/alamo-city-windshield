---
phase: 03-vin-estimate
plan: 08
type: execute
wave: 6
depends_on: [03-07]
files_modified: []
autonomous: false
requirements: [VIN-01, VIN-02, VIN-03, VIN-04, VIN-05, VIN-06, VIN-07]
user_setup: []

must_haves:
  truths:
    - "A human has confirmed all five ROADMAP Phase 3 success criteria against the running application"
    - "A human has confirmed the three failure modes produce three distinct screens"
    - "A human has confirmed the estimate section still snap-scrolls correctly on a mobile viewport"
  artifacts: []
  key_links: []
---

<objective>
Verify the completed VIN estimate feature against the five ROADMAP success criteria and the three distinct failure paths, in a real browser, on both desktop and mobile viewports.

Purpose: Every prior plan verified its own slice. This checkpoint verifies the phase's actual promise end to end — including the two criteria that are branches rather than the happy path (the fallback for a failed lookup, and the 2018+ ADAS notice) and the three-way failure distinction that CONTEXT.md calls out as a regression risk. It also confirms the snap-scroll experience survived the D-07 in-card swap, which is the one thing no automated check in this phase can see.

Output: A human verification record. No files change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-vin-estimate/03-CONTEXT.md
@.planning/phases/03-vin-estimate/03-07-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify the five phase success criteria and the three failure paths in a browser</name>
  <read_first>
    - `.planning/ROADMAP.md` §Phase 3 — the five success criteria this checkpoint is the sole judge of
    - `.planning/phases/03-vin-estimate/03-07-SUMMARY.md` — which view states the executor already exercised and what they observed
    - `.planning/phases/03-vin-estimate/03-CONTEXT.md` — decisions D-06 (the six approved quotes), D-07, D-08, D-17, D-18, D-19, D-20
  </read_first>
  <action>
    Do not modify any file in this plan. Start the dev server, walk the developer through the 20 verification steps below covering all five ROADMAP Phase 3 success criteria and the three distinct failure paths, and record a pass or fail verdict per criterion along with the developer's verbatim response. If any step fails, capture exactly what was observed so it can feed `/gsd:plan-phase 03 --gaps`.
  </action>
  <what-built>
    The complete VIN estimate flow, replacing the Phase 2 placeholder:
    - `GET /api/vin/[vin]` — validates the VIN server-side, reads `vin_cache`, calls NHTSA vPIC with a 6-second timeout, writes successful decodes back to the cache, and returns the vehicle identity plus all nine precomputed price variants
    - `GET /api/estimate?year=YYYY` — the same nine variants from a model year alone, for the manual-entry path
    - `src/lib/pricing.ts` — the locked formula, server-only, with all six approved D-06 quotes encoded as passing tests
    - `src/components/home/EstimateResult.tsx` — the range, the four-row breakdown, the glass selector with per-option hints, the vehicle-type selector when needed, the ADAS notice, the CTA, and the disclaimer
    - `src/components/home/ManualEntryForm.tsx` — the two-field fallback
    - `src/components/home/EstimateSection.tsx` — rewired to the real lookup with five view states

    Before pausing for you, the executor confirmed: `npm test` passes (all six D-06 fixtures plus the classifier suite), `npm run build` succeeds, `npm run lint` passes, `npx tsc --noEmit` passes, and the `curl` checks against both Route Handlers behave as specified.
  </what-built>
  <how-to-verify>
    Start the dev server with `npm run dev` and open http://localhost:3000. Scroll to the second snap section (the estimate card).

    **Criterion 1 — valid VIN returns identity plus a range.**
    1. Enter `1FTFW1E85NFA12345` and submit. Confirm the result appears in a few seconds reading **2022 Ford F-150** with a range of **$338 – $663**.

    **Criterion 2 — range with a visible line-item breakdown.**
    2. On that result, confirm four breakdown rows are visible without any clicking: a base replacement row, a vehicle size row, a glass type row, and a camera recalibration row reading **up to $250**.

    **Criterion 3 — glass selector updates the estimate.**
    3. Open DevTools → Network. Click **Acoustic**. Confirm the range changes to **$428 – $773**, the hint line below the selector changes to describe acoustic glass, and **no new network request appears**.
    4. Click **Heated**. Confirm the range changes to **$518 – $883**.
    5. Click back to **Standard** and confirm the range returns to **$338 – $663**.

    **Criterion 5 — ADAS notice for 2018 and later.**
    6. Confirm a note is visible below the breakdown explaining that vehicles from 2018 or later often have a camera behind the windshield and that recalibration cost is included in the upper estimate.

    **D-07 — swap and return.**
    7. Confirm the VIN input is gone (replaced in place, not pushed below) and that an "Estimate another vehicle" affordance is present. Click it and confirm the form returns empty.

    **D-08 — loading state.**
    8. Submit a valid VIN again and watch the button. Confirm it becomes a spinner reading **Decoding VIN…**, the input is locked while it spins, and **the card does not change height or jump**.

    **Failure path 1 of 3 — D-18, VIN rejected.**
    9. Enter `ZZZZZZZZZZZZZZZZZ` and submit. Confirm the VIN form **stays visible and editable**, a message below it asks you to double-check the VIN characters, and a secondary option to enter details manually is offered. Confirm this message is **not** styled as a hard red validation error and that the manual form did **not** open by itself. This is the path the developer specifically asked to be distinct — a mistyped VIN must read as a fixable typo, not get absorbed by the fallback.

    **Failure path 2 of 3 — D-17/D-20, service unreachable, and Criterion 4.**
    10. Click the manual-entry option from step 9. Confirm a form appears asking for only **two** things: a model year and a vehicle type with exactly three choices — Car, SUV or Truck, Van. Confirm there is no make or model field.
    11. Enter year `2015`, choose **Car**, submit. Confirm a result appears reading **2015 Car** with **$270 – $330**, that the camera recalibration row now says it is not required for this vehicle, that there is **no** ADAS note, and that one line notes the estimate is based on the details you entered.
    12. Confirm the vehicle type is still a live selector on this result. Switch it to **Van** and confirm the range changes to **$405 – $495** and the headline changes to **2015 Van**.
    13. Now test the real unreachable path: in DevTools → Network, set throttling to **Offline**. Return to the VIN form, enter `1FTFW1E85NFA12345`, and submit. Confirm the **manual entry form appears** — not an error message, not a red alert, not a blank card. Set throttling back to No throttling afterward. This is ROADMAP criterion 4.

    **Failure path 3 of 3 — D-19, vehicle type undeterminable.**
    14. This path fires when NHTSA decodes a vehicle whose body class is not one of the three size buckets — for example a `Truck-Tractor` or an `Incomplete` chassis. It is covered by unit assertions in `npm test`, but confirm the UI shape by observing steps 11-12: the vehicle-type selector shown there is the same control D-19 surfaces, appearing alongside the glass selector with Car preselected. Confirm you are satisfied that when the VIN decodes but the type cannot be determined, the app asks about exactly that one value rather than guessing.

    **Disclaimer and CTA.**
    15. On any result, confirm a muted one-line disclaimer under the CTA states this is an estimate rather than a final quote, that pricing is confirmed on seeing the vehicle, and that it shows the real business phone number `(210) 555-0100`.
    16. Confirm the **Book Appointment** button is present and navigates to `/contact`.

    **Placeholder removal — D-12.**
    17. Confirm the text "Estimates launching soon" appears nowhere, and that no result ever shows "2024 Toyota Camry" or "$250 – $400".

    **Mobile and snap scroolling.**
    18. Switch to a 375×667 device viewport. Repeat steps 1, 2, 3, 9, and 11. For each, confirm the card fits within the viewport without the page scrolling inside the section, and that swiping still snaps cleanly between the hero, the estimate section, and the services section in both directions.
    19. Confirm both selectors are tappable at that width and that the three vehicle-type labels are not truncated.

    **Caching — VIN-03.**
    20. Optional but valuable: in the Supabase dashboard, open the `vin_cache` table. Confirm a row exists for `1FTFW1E85NFA12345` and that **no** row exists for `ZZZZZZZZZZZZZZZZZ`. Failed lookups must never be cached.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms `1FTFW1E85NFA12345` yields "2022 Ford F-150" with the range $338 – $663 (ROADMAP criterion 1)
    - Human confirms four breakdown rows are visible with no interaction (ROADMAP criterion 2)
    - Human confirms the glass selector changes the range to $428 – $773 for Acoustic and $518 – $883 for Heated with no network request (ROADMAP criterion 3, VIN-06)
    - Human confirms the ADAS note appears for the 2022 vehicle and does not appear for the 2015 manual entry (ROADMAP criterion 5, VIN-07)
    - Human confirms an offline network state produces the manual entry form and not an error state (ROADMAP criterion 4, VIN-02)
    - Human confirms `ZZZZZZZZZZZZZZZZZ` keeps the form visible with a double-check-the-characters message and does NOT auto-open the manual form (D-18 distinctness)
    - Human confirms the manual form has exactly two fields and no make or model input (D-17)
    - Human confirms the manual result reads "2015 Car" at $270 – $330 with the "based on the details you entered" line (D-20)
    - Human confirms the vehicle-type selector is live on the manual result and switching to Van yields $405 – $495 (D-19 pattern)
    - Human confirms the loading state is an in-button spinner reading "Decoding VIN…" with no card height change (D-08)
    - Human confirms the result replaces the form inside the same card and "Estimate another vehicle" returns to it (D-07)
    - Human confirms the disclaimer shows `(210) 555-0100` and the CTA navigates to `/contact` (D-11)
    - Human confirms "Estimates launching soon", "2024 Toyota Camry", and "$250 – $400" appear nowhere (D-12)
    - Human confirms snap scrolling still works in both directions at 375×667 and the card fits the viewport
    - If the optional cache check was performed: a `vin_cache` row exists for the valid VIN and none exists for the rejected VIN (VIN-03, D-21)
  </acceptance_criteria>
  <done>All five ROADMAP Phase 3 success criteria have a recorded pass or fail verdict from direct browser observation on both desktop and a 375x667 mobile viewport, and the three failure modes are confirmed to produce three distinct screens.</done>
  <resume-signal>Type "approved" if every check passed, or list the step numbers that failed along with what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| running application → human reviewer | Observation only; no code or configuration changes in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Information Disclosure | The deployed client bundle | mitigate | Step 3's Network-tab observation doubles as the human confirmation of D-15: because switching glass type changes the price with zero network requests and the pricing constants are absent from the bundle, the browser demonstrably holds precomputed numbers rather than the formula. `npm run build` succeeding in plan `03-07` is the build-time proof; this is the observable one. |
| T-03-08 | Information Disclosure | Failure-path UI copy | mitigate | Steps 9 and 13 confirm that neither the rejected-VIN path nor the offline path renders a stack trace, HTTP status, or raw error string — only the fixed copy from `ESTIMATE_COPY`. |
| T-03-15 | Denial of Service | `/api/vin/[vin]` as an unrate-limited public proxy | accept | Unchanged from plan `03-06`: explicitly deferred by the developer per `03-CONTEXT.md` `<deferred>`. This checkpoint does not test it and must not be used to justify adding rate limiting to this phase. Carried forward as a documented residual risk for a future phase. |
</threat_model>

<verification>
A human has walked all 20 steps against the running application on both a desktop and a 375×667 mobile viewport, and has either approved or enumerated the failing step numbers.
</verification>

<success_criteria>
All five ROADMAP Phase 3 success criteria are confirmed satisfied by direct observation, the three failure modes are confirmed to produce three distinct screens, and the snap-scroll experience is confirmed intact on mobile.
</success_criteria>

<output>
Create `.planning/phases/03-vin-estimate/03-08-SUMMARY.md` when done. Record each of the five ROADMAP criteria with a pass or fail verdict and the human's verbatim response. Any failure becomes gap-closure input for `/gsd:plan-phase 03 --gaps`.
</output>
